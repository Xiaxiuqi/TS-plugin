<analysis>
  <context>已确认 public/story_ui_lite_test 只有一个 db-status-bar 源码副本：public/story_ui_lite_test/modules/db-status-bar/style.css 与 index.js。loader.js 与 preview-db-status.html 都加载该 style.css。搜索 public/src 未发现另一个 db-status-bar 构建样式；preview-db-status.html 的 .preview-controls 精确包含 padding:8px 0 12px 与 flex-wrap:wrap，但它不是 .db-sb-fn-toolbar。当前 style.css:1140-1145 的 .db-sb-fn-layout 是 grid 且只有一列、min-height:300px，没有 grid-template-rows；style.css:1295-1299 的 .db-sb-map-viewport 没有 min-height。index.js:904-966 的 doMap(root,false) 在无缓存且不生成时显示纯空状态；index.js:1038-1041 的 AI 失败无旧图也显示纯空状态；index.js:1066-1095 的 renderMapTab 初始 viewport 固定渲染“暂无地图缓存”。data.js:249-254 的 mapElements 含 name/type/x/y/status/desc/area/lore，足够生成确定性基础 SVG。</context>
  <needs>需要修复源码侧真实缺陷：工具栏不能被 grid 行拉伸，地图区域即使空状态也要有可见高度，无 AI 缓存时基于数据库 mapElements 展示非 AI 基础 SVG。运行时旧 toolbar computed style 若仍出现，证据更指向浏览器缓存或误读 preview-controls，而不是另一个源码文件。</needs>
  <key_challenges>不能恢复静态默认地图；不能把基础 SVG 写入 mapCache；不能让普通刷新触发 AI；不能修改 data.js/loader/preview；不能为了兜底伪造不存在的地图元素。</key_challenges>
  <confidence>HIGH：加载链路、选择器、doMap 分支、renderMapTab 初始渲染、mapElements 字段均已读取或搜索确认；剩余风险是浏览器缓存导致实测 CSS 不更新。</confidence>
  <approach>推荐方案是在 CSS 增加布局行契约和 viewport 最小高度，在 JS 增加 renderBaseMap(S) 并仅用于初始、无缓存不生成、AI失败无旧图路径。可维护性9/10：职责集中在地图渲染和布局契约。健壮性8/10：覆盖无缓存、生成关闭、AI失败、无元素空状态。可扩展性8/10：未来 mapElements 增字段时可在同一 renderer 扩展。</approach>
  <edge_cases>1. mapElements 为空时显示“暂无地图元素”。2. x/y 缺失时按索引环形排布。3. x/y 超出 0-800/0-600 时夹紧。4. 名称含特殊字符时用 esc 写入 SVG 文本。5. doMap(root,false) 不调用 AI。6. AI 失败且无旧图时显示基础 SVG但不写 mapCache。</edge_cases>
  <affected_scope>public/story_ui_lite_test/modules/db-status-bar/style.css；public/story_ui_lite_test/modules/db-status-bar/index.js；.analysis-cache.md；.analysis-archive/验收归档文件。</affected_scope>
  <execution_plan>1. 写入 .analysis-cache.md。2. 修改 style.css 的 .db-sb-fn-layout、.db-sb-map-area、.db-sb-map-container、.db-sb-map-viewport。3. 在 index.js 的 renderMapEmptyState 后加入 renderBaseMap。4. 修改 renderMapTab 初始 viewport 使用 renderBaseMap(S) 或空状态。5. 修改 doMap 无缓存不生成与 AI失败无旧图分支使用基础 SVG。6. 运行 node --check、关键词搜索和独立验收。</execution_plan>
  <degradation_check>YES 不改 AI 触发策略：doMap(root,false) 仍不生成。YES 不伪造默认地图：基础 SVG只来自 S.mapElements。YES 不写 mapCache：只有 AI成功或保留旧SVG路径仍写缓存。YES 修改范围符合限制。YES CSS 修复针对 122px/20px 根因。NO 不能保证浏览器立即采用新 CSS，若缓存存在需硬刷新。</degradation_check>
</analysis>

---
<decision_point>
  <issue>独立验收发现两个必须修复项和一个布局语义风险：index.js 在 AI 失败且保留 previousSvg 时仍可能把旧 AI SVG 用当前 signature 写入 mapCache；renderBaseMap 生成的 SVG 直接 innerHTML 写入，没有统一经过 sanitizeSVG；style.css 的 align-items:start 避免 toolbar 被拉伸，但 .db-sb-map-area 未显式 align-self:stretch，地图区域可能只靠内容高度撑开。</issue>
  <impact>如果不修正，地图元素变化后 AI 重绘失败可能把旧图伪装成新签名缓存，后续刷新会误判缓存有效；基础 SVG 安全边界依赖当前字段转义和固定颜色，未来字段扩展时容易被击穿；地图区域布局虽然不再 20px，但填充剩余高度的意图仍不够明确。</impact>
  <context_update>已通过 node --check；关键词搜索未发现“锻刀室/默认地图”；renderBaseMap 当前位于 public/story_ui_lite_test/modules/db-status-bar/index.js:888-916；失败保留旧图分支位于 index.js:1079-1089；CSS grid 和 viewport修改位于 public/story_ui_lite_test/modules/db-status-bar/style.css:1140-1149 与 1300-1320。</context_update>
  <options><option_a><description>只修缓存污染。</description><approach_evaluation>可维护性7/10；健壮性6/10；可扩展性6/10。</approach_evaluation><edge_cases>AI失败保留旧图；基础SVG安全；地图区域高度。</edge_cases><affected_scope_delta>只改index.js。</affected_scope_delta></option_a><option_b><description>修缓存污染，基础SVG走sanitizeSVG，map-area明确stretch。</description><approach_evaluation>可维护性9/10；健壮性9/10；可扩展性8/10。</approach_evaluation><edge_cases>基础SVG清洗；data-idx范围；AI失败旧图不改缓存；地图区域填满第二行。</edge_cases><affected_scope_delta>仍只改index.js与style.css。</affected_scope_delta></option_b><option_c><description>大幅重构地图渲染链路。</description><approach_evaluation>可维护性6/10；健壮性8/10；可扩展性7/10。</approach_evaluation><edge_cases>空状态误清洗；AI SVG属性；旧缓存迁移。</edge_cases><affected_scope_delta>扩大doMap和setMapViewportMarkup修改面。</affected_scope_delta></option_c></options>
  <recommendation>选择 option_b。</recommendation>
  <execution_plan_update>追加decision_point；修改renderBaseMap返回sanitizeSVG(svg)||''；删除失败保留旧图时setMapCacheEntry；给.db-sb-map-area增加align-self:stretch；重新验收。</execution_plan_update>
  <deviation_audit>YES 偏离原计划但有独立验收证据；YES 仍在affected_scope内；YES 不改变AI策略和fallback边界。</deviation_audit>
  <degradation_check>YES 不用shell修改源码。YES 不写基础SVG缓存。YES 不改变普通刷新策略。YES 不恢复默认地图。</degradation_check>
</decision_point>


---
<verification>
  <syntax_check>PASS：cmd 执行 node --check public/story_ui_lite_test/modules/db-status-bar/index.js，exitCode=0。</syntax_check>
  <keyword_check>PASS：搜索“锻刀室|默认地图|暂无地图缓存”无命中；“未写入伪造地图缓存”仅命中 public/story_ui_lite_test/modules/db-status-bar/index.js:1096 的允许通知文案。</keyword_check>
  <cache_path_check>PASS：setMapCacheEntry 仅出现在缓存规范化与 AI 成功生成路径；AI 失败保留 previousSvg 分支未写入当前 signature 缓存。</cache_path_check>
  <ai_policy_check>PASS：doMap(root,false) 由按钮刷新触发时 allowGenerate=false，shouldTryGenerate=false，会在非生成分支 return；callMapAI 只位于 shouldTryGenerate 后的生成分支。</ai_policy_check>
  <css_contract_check>PASS：.db-sb-fn-layout 使用 auto/minmax 行契约与 align-items:start；.db-sb-map-area 单独 align-self:stretch；.db-sb-map-viewport 与 .db-sb-map-empty 均有 min-height:200px。</css_contract_check>
  <independent_review>PASS：高性能模型只读复审结论为通过，无必须修复项；建议观察 lastAutoMapSignature 更新节奏，但不构成本次阻断。</independent_review>
  <diff_stat>public/story_ui_lite_test/modules/db-status-bar/index.js 与 style.css 共 2 个文件变更，151 insertions(+)，25 deletions(-)。</diff_stat>
  <remaining_external_risk>浏览器可能仍缓存 style.css?v=lite_test-0.1.0；若硬刷新后仍旧样式，需要助手确认是否允许修改 loader.js 版本号，这超出本次限定范围。</remaining_external_risk>
</verification>

---
<output_quality_review>
  <task_summary>已完成 db-status-bar 地图页布局与无缓存基础地图修复的最终验收闭环。</task_summary>
  <deliverables>业务代码修改限定在 public/story_ui_lite_test/modules/db-status-bar/index.js 与 public/story_ui_lite_test/modules/db-status-bar/style.css；验证证据已追加到 .analysis-cache.md 并准备归档。</deliverables>
  <metrics>total_files_modified=2；execution_plan_coverage=100%；edge_cases_handled=mapElements为空、坐标缺失、坐标越界、特殊字符转义、doMap(root,false)不调用AI、AI失败无旧图显示基础图或空状态；confidence_assessment=HIGH。</metrics>
  <substance_check>基础 SVG 来自 S.mapElements 并经过 sanitizeSVG；未恢复静态默认地图；未把基础 SVG 写入 mapCache；CSS 直接修复 grid 行高拉伸与 viewport 20px 退化根因。</substance_check>
  <completeness_check>affected_scope 内 index.js 与 style.css 均已验证；浏览器 computed height 仍需在 SillyTavern 硬刷新后实测。</completeness_check>
  <value_density_check>高价值内容集中在布局契约、基础地图回退、安全清洗、缓存语义与 AI 调用边界；无不必要跨模块重构。</value_density_check>
  <alignment_check>满足用户本质需求；如果这是别人交给我的，我会接受。外部风险仅剩浏览器缓存。</alignment_check>
</output_quality_review>
