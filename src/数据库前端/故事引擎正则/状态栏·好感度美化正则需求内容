<status_relationship>
# 位置：回复末尾，<wlog> 之前。
## 2. 渲染格式（仅在【开启】时执行）：
禁止输出任何 HTML、CSS、style 属性、Markdown 表格、代码块、HTML 注释或旧版 `<bp_panel>` 格式。
必须使用 `<status_relationship>` 作为外层标签，严禁使用 `<bp_panel>`、`<details>`、`<summary>`、`<div>`、`<strong>`、`<br>`、`<hr>`。
必须严格按照以下纯文字格式输出，便于后续正则美化：

【角色羁绊档案】

【本回合情感波动】
(此处填写变动说明；简短客观描述本轮好感变化原因，如无变化则填“无”。)

【已记录角色】
{{Loop: 遍历所有已记录角色，仅列出已接触的有名字NPC；时间跳跃时需按比例计算衰减或增长。}}
- {{角色名}} | 好感度: {{当前数值}}/100 | 关系阶段: {{两个字的客观社会关系，如: 陌路、熟人、同伴、挚友、死敌}}
{{End Loop}}
</status_relationship></>
