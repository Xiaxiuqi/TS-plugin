# 故事引擎悬浮球美化正则

## 正则用途

将 `<story_driver>...</story_driver>` 输出渲染为楼层内居中的可折叠悬浮球入口。点击悬浮球后展开米白色低饱和面板，并由轻量内嵌脚本解析完整故事引擎内容，结构化展示：

- 全域摘要：时空、天气、异常拦截、NPC情报盲区、死亡角色、蝴蝶效应、故事主线、原作走向。
- 事件表：自动解析 `[变数注入]` 下的事件A到事件J或事件1到事件10。
- 事件融合分析：自动解析 `①②③④` 与分析段。
- 行为逻辑锁：单独显示玩家输入意图、持有检定、物理检定、修正结果。
- NPC驱动：每个 `<npc_driver>` 自动解析成独立卡片。
- 战斗驱动：兼容“无”和完整战斗内容。
- 最终修正：单独显示故事导演内容。

## 设计约束

- 楼层内居中：悬浮球不使用 `position: fixed`，不会显示到楼层外部。
- 可收起：使用原生 `<details>` / `<summary>` 控制展开与收起。
- 脚本轻量：只在当前楼层内解析 `$1` 的完整 `<story_driver>` 文本，不使用外部依赖、不读写 `localStorage`、不注册全局事件。
- 支持不固定数量 `<npc_driver>`：脚本会扫描全部 `<npc_driver>...</npc_driver>` 并生成网格卡片。
- 支持 `<combat_driver>` 有内容与无内容。
- 禁止 emoji：仅使用 SVG 图标以及 ✦、✧。
- 悬浮球已缩小为 56px，内圈 34px，避免占据过多楼层空间。

## 查找正则表达式

### 酒馆正则面板推荐填写版本（方案C：完整捕获 + 轻量脚本解析）

当前版本只捕获完整 `<story_driver>...</story_driver>`，再由替换HTML中的轻量脚本在楼层内解析。这样比14组正则更稳定，也能完整显示事件表和NPC卡片。

```regex
/(<story_driver>[\s\S]*?<\/story_driver>)/s
```

占位符对应关系：

- `$1`：完整 `<story_driver>...</story_driver>` 原文，由新版替换内容中的脚本解析。
- 替换内容必须使用 `src/数据库前端/故事引擎正则/故事引擎悬浮球美化正则-替换内容-纯HTML` 的全部纯HTML内容。
- 不要复制本说明文件中的代码块围栏。

### 兜底诊断版

如果推荐版不触发，请临时用这一版测试正则范围是否作用到AI楼层，替换内容临时填 `命中故事引擎`：

```regex
/<story_driver>\s*(.*?)\s*<\/story_driver>/s
```

若兜底诊断版也不触发，则问题不在替换HTML，而是正则启用状态、作用范围、执行阶段，或其他正则提前处理了 `<story_driver>`。

## 替换内容

当前主方案的纯HTML替换内容已单独放在：

`src/数据库前端/故事引擎正则/故事引擎悬浮球美化正则-替换内容-纯HTML`

请复制该文件全部内容作为替换内容。不要复制 Markdown 代码块围栏。

## 使用建议

1. 在酒馆正则中新增脚本，匹配目标选择 AI 输出或消息显示阶段。
2. 查找正则填写“酒馆正则面板推荐填写版本（方案C：完整捕获 + 轻量脚本解析）”。
3. 不要使用 `/.../g`，当前参考环境更适合 `/.../s`。
4. 替换内容复制 `src/数据库前端/故事引擎正则/故事引擎悬浮球美化正则-替换内容-纯HTML` 的全部内容。
5. 若纯HTML文件在VSCode中显示CSS报错，可忽略；这是因为文件无 `.html` 扩展名导致编辑器误判语言，不影响复制到酒馆正则替换框。
6. 如果渲染后出现“无事件表”或“无NPC驱动”，优先检查AI原文是否仍保留 `[变数注入]:`、`<npc_driver>`、`<combat_driver>` 标签。

## 自检确认

- [x] 符合楼层内显示，不使用外部固定悬浮窗。
- [x] 使用轻量内嵌脚本解析完整 `<story_driver>`。
- [x] 支持事件表结构化显示。
- [x] 支持事件融合分析显示。
- [x] 支持一个或多个 `<npc_driver>` 自动生成网格卡片。
- [x] 支持 `<combat_driver>` 为“无”或完整内容。
- [x] 面板可点击展开与收起。
- [x] 禁止 emoji，仅使用 SVG 图标以及 ✦、✧。
- [x] 采用米白底与低饱和重点色。
