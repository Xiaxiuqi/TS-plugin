# 变量更新美化正则-暗色版本

## 使用目标

本文件用于替换原 `变量更新美化正则` 的深色内联样式，将变量更新内容渲染为与当前暗色版状态栏一致的深色终端折叠面板。

适配原有变量更新正则的替换占位：

```text
$2 = 变量更新正文内容
```

## 正则执行顺序

只需要创建并启用一条正则：

1. `变量更新折叠面板_暗色`

建议设置为作用于 AI 输出或消息显示阶段。若当前已有变量更新正则，只替换“替换内容”即可，查找正则沿用原规则。

---

## 变量更新折叠面板_暗色

### 查找正则

沿用当前 `变量更新美化正则` 已配置的查找正则。

若需要重新创建，可使用你当前变量更新标签/代码块对应的原查找表达式，并确保正文内容捕获为 `$2`。

### 替换内容

```html
<div class="var-update-widget var-update-hsr-ui">
  <style>
    .var-update-widget.var-update-hsr-ui {
      --vu-space: #171b22;
      --vu-panel: rgba(29, 35, 44, .97);
      --vu-panel-2: rgba(38, 45, 56, .92);
      --vu-row: rgba(255, 255, 255, .055);
      --vu-row-2: rgba(255, 255, 255, .082);
      --vu-text: #ede7dc;
      --vu-muted: #b8afa1;
      --vu-faint: #807d78;
      --vu-gold: #c8a86c;
      --vu-cyan: #88aeb8;
      --vu-blue: #8091ad;
      --vu-rose: #b9877e;
      --vu-shadow: 0 4px 10px rgba(0, 0, 0, .16);
      --vu-shadow-soft: 0 2px 6px rgba(0, 0, 0, .12);
      width: 100%;
      margin: .8em auto;
      display: flex;
      justify-content: center;
      color: var(--vu-text);
      font-family: "Noto Serif SC", "Source Han Serif SC", "Microsoft YaHei", serif;
      line-height: 1.5;
      box-sizing: border-box;
    }

    .var-update-widget.var-update-hsr-ui,
    .var-update-widget.var-update-hsr-ui * {
      box-sizing: border-box;
    }

    .var-update-widget.var-update-hsr-ui details {
      width: min(96%, 900px);
      overflow: hidden;
      border-radius: 8px;
      background:
        linear-gradient(135deg, rgba(200,168,108,.1), transparent 22%),
        radial-gradient(circle at 90% 0%, rgba(136,174,184,.12), transparent 30%),
        linear-gradient(180deg, var(--vu-panel), var(--vu-space));
      box-shadow: var(--vu-shadow);
      position: relative;
    }

    .var-update-widget.var-update-hsr-ui details::before {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      background:
        linear-gradient(90deg, rgba(200,168,108,.28), transparent 18%, transparent 82%, rgba(136,174,184,.18)),
        linear-gradient(180deg, rgba(255,255,255,.055), transparent 80px);
      opacity: .58;
      z-index: 0;
    }

    .var-update-widget.var-update-hsr-ui summary {
      list-style: none;
    }

    .var-update-widget.var-update-hsr-ui summary::-webkit-details-marker {
      display: none;
    }

    .var-update-widget.var-update-hsr-ui .vu-summary {
      width: 100%;
      min-height: 38px;
      cursor: pointer;
      user-select: none;
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 9px;
      align-items: center;
      padding: 10px 12px;
      color: var(--vu-text);
      background:
        linear-gradient(90deg, rgba(200,168,108,.14), rgba(136,174,184,.06), transparent),
        rgba(255,255,255,.035);
      transition: background .22s ease, filter .22s ease;
      position: relative;
      z-index: 1;
    }

    .var-update-widget.var-update-hsr-ui .vu-summary:hover {
      filter: brightness(1.08);
      background:
        linear-gradient(90deg, rgba(200,168,108,.2), rgba(136,174,184,.1), transparent),
        rgba(255,255,255,.055);
    }

    .var-update-widget.var-update-hsr-ui .vu-toggle-icon {
      width: 31px;
      height: 31px;
      border-radius: 6px;
      display: grid;
      place-items: center;
      color: var(--vu-gold);
      background: rgba(255,255,255,.06);
      font-size: 15px;
      font-weight: 900;
    }

    .var-update-widget.var-update-hsr-ui .vu-summary-text {
      min-width: 0;
    }

    .var-update-widget.var-update-hsr-ui .vu-toggle-title {
      display: block;
      margin: 0;
      color: var(--vu-text);
      font-size: 13px;
      font-weight: 900;
      letter-spacing: .12em;
    }

    .var-update-widget.var-update-hsr-ui .vu-toggle-subtitle {
      display: block;
      margin-top: 1px;
      color: var(--vu-muted);
      font-size: 10px;
      font-weight: 600;
      letter-spacing: .1em;
    }

    .var-update-widget.var-update-hsr-ui .toggle-icon {
      color: var(--vu-gold);
      transition: transform .24s ease;
      flex: 0 0 auto;
      font-size: 12px;
      letter-spacing: 0;
      opacity: 1;
    }

    .var-update-widget.var-update-hsr-ui details:not([open]) .toggle-icon {
      transform: rotate(-90deg);
    }

    .var-update-widget.var-update-hsr-ui .vu-panel {
      width: 100%;
      overflow: hidden;
      border: none;
      background: transparent;
      box-shadow: none;
      position: relative;
      z-index: 1;
    }

    .var-update-widget.var-update-hsr-ui .vu-body {
      padding: 10px 12px 12px;
      display: grid;
      gap: 8px;
    }

    .var-update-widget.var-update-hsr-ui .vu-card {
      min-width: 0;
      border-radius: 6px;
      border: none;
      background: var(--vu-panel-2);
      box-shadow: var(--vu-shadow-soft);
      overflow: hidden;
      position: relative;
    }

    .var-update-widget.var-update-hsr-ui .vu-card::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      width: 3px;
      height: 100%;
      background: var(--vu-rose);
      opacity: .72;
    }

    .var-update-widget.var-update-hsr-ui .vu-card-head {
      padding: 8px 10px 7px 12px;
      display: flex;
      align-items: center;
      gap: 8px;
      background: linear-gradient(90deg, rgba(255,255,255,.062), transparent);
      position: relative;
      z-index: 1;
    }

    .var-update-widget.var-update-hsr-ui .vu-card-dot {
      width: 14px;
      height: 2px;
      border-radius: 2px;
      flex: 0 0 auto;
      background: var(--vu-gold);
      box-shadow: 10px 0 0 rgba(200,168,108,.32);
    }

    .var-update-widget.var-update-hsr-ui .vu-card-title {
      color: var(--vu-text);
      font-size: 12px;
      font-weight: 800;
      letter-spacing: .11em;
    }

    .var-update-widget.var-update-hsr-ui .vu-content {
      max-height: 320px;
      overflow-y: auto;
      padding: 9px 10px 10px 12px;
      color: var(--vu-text);
      background: rgba(255,255,255,.025);
      white-space: pre-wrap;
      word-break: break-word;
      overflow-wrap: anywhere;
      font-size: 12.5px;
      line-height: 1.64;
      scrollbar-width: thin;
      scrollbar-color: rgba(200,168,108,.54) rgba(0,0,0,.22);
      position: relative;
      z-index: 1;
    }

    .var-update-widget.var-update-hsr-ui .vu-content::-webkit-scrollbar {
      width: 6px;
    }

    .var-update-widget.var-update-hsr-ui .vu-content::-webkit-scrollbar-track {
      background: rgba(0,0,0,.22);
      border-radius: 999px;
    }

    .var-update-widget.var-update-hsr-ui .vu-content::-webkit-scrollbar-thumb {
      background: rgba(200,168,108,.54);
      border-radius: 999px;
    }

    .var-update-widget.var-update-hsr-ui .vu-panel-foot {
      padding: 8px 14px 10px;
      text-align: center;
      color: var(--vu-faint);
      font-size: 10px;
      letter-spacing: .14em;
      background: rgba(0,0,0,.16);
    }

    @media (max-width: 640px) {
      .var-update-widget.var-update-hsr-ui details {
        width: 98%;
      }

      .var-update-widget.var-update-hsr-ui .vu-content {
        max-height: 260px;
      }
    }
  </style>

  <details>
    <summary class="vu-summary" aria-label="展开或收起变量更新">
      <span class="vu-toggle-icon">✦</span>
      <span class="vu-summary-text">
        <span class="vu-toggle-title">变量更新</span>
        <span class="vu-toggle-subtitle">VARIABLE UPDATE TERMINAL</span>
      </span>
      <span class="vu-toggle-state toggle-icon">▼</span>
    </summary>

    <section class="vu-panel">
      <div class="vu-body">
        <article class="vu-card">
          <div class="vu-card-head"><span class="vu-card-dot"></span><span class="vu-card-title">更新明细</span></div>
          <div class="vu-content">$2</div>
        </article>
      </div>

      <footer class="vu-panel-foot">✧ VARIABLE UPDATE TERMINAL ✧</footer>
    </section>
  </details>
</div>
```

---

## 注意事项

1. 本方案不使用脚本，适合酒馆正则替换渲染环境。
2. 替换内容保留 `$2`，可直接替换原 `变量更新美化正则` 的替换内容。
3. 标题栏左侧图标沿用当前统一的方形图标风格，右侧箭头沿用 `▼` 折叠旋转逻辑。
4. 内容区使用 `max-height: 320px` 与内部滚动条，避免变量更新过长时撑开消息。
