# 项目级行尾约定：转换工具按字节搬运 content，要求源文件统一为 LF + UTF-8
* text=auto eol=lf

*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf

# 二进制例外（如未来加入截图等）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
