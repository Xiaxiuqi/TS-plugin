# 地图模块完全简化 v8.8

## 删除的功能

### 1. 编辑模式
- ❌ "编辑"按钮
- ❌ `state.isEditing` 标志
- ❌ 编辑模式切换逻辑
- ❌ 编辑模式下的拖拽功能

### 2. 侧边栏
- ❌ 元素管理侧边栏 `#ci-elements-sidebar`
- ❌ `state.elementsSidebar` 标志  
- ❌ `renderElementsSidebar()` 函数
- ❌ 侧边栏CSS (约200行)
- ❌ 侧边栏拖拽到地图功能

### 3. MapLayout保存
- ❌ `state.mapLayout` 对象
- ❌ `STORAGE_MAP_LAYOUT_KEY` 常量
- ❌ `saveMapLayout()` 函数
- ❌ 地点/元素位置保存逻辑
- ❌ 地点尺寸调整功能
- ❌ 右下角拖拽点

### 4. 容器管理
- ❌ 容器整理功能
- ❌ `state.containers` 对象
- ❌ `STORAGE_CONTAINERS_KEY` 常量
- ❌ 所有容器相关函数(10+个)
- ❌ 容器CSS样式

## 保留的功能

### ✅ 1. 地图显示
- 地点矩形 + 名称文字
- 自动布局(避免重叠)
- viewBox自动计算
- 主角位置标记(脉冲波动画)

### ✅ 2. 元素显示
- 30x30矩形 + 下方文字
- 伪随机位置(基于地点名称hash)
- 自动分布在地点内

### ✅ 3. 互动功能
- 点击地点 → "前往XX"
- 点击元素 → 显示互动选项
- 互动选项来自数据库的"互动选项1/2/3"

### ✅ 4. 地图操作
- 鼠标拖拽平移
- 滚轮缩放
- 缩放按钮(+/-)

## 标准表格模板

### 主要地点表
```
列: 地点名称 | X坐标 | Y坐标 | 宽度 | 高度 | 环境描述
```

**SourceData**:
- note: "记录当前活动层级的具体地点。当地点层级深入时（如从'小区'进入'公寓楼'），此表会被清空并填充新层级的子地点。"
- initNode: "游戏初始化时，需为当前层级区域新增至少三个主要地点。"
- deleteNode: "当发生地点层级深入时，原表中的地点在移至'外部区域列表'后将被删除。"
- updateNode: "地点的环境描述等信息发生变化时更新。"
- insertNode: "在当前层级内发现新地点时添加。"

### 地图元素表
```
列: 元素名称 | 元素类型 | 元素描述 | 所属主地点 | 状态 | 互动选项1 | 互动选项2 | 互动选项3
```

**SourceData**:
- note: "记录场景中可交互的实体（怪物/NPC/物品）。`所属主地点`必须与主要地点表对应。"
- initNode: "新地点创建时，必须为其添加至少一个地图元素。"
- deleteNode: "实体被消灭/摧毁/取走，或者普通NPC因为剧情发展变成剧情重要角色时删除。"
- updateNode: "实体状态因交互改变时更新。每轮必须根据最新情景刷新所有互动选项。"
- insertNode: "场景中出现新的可交互实体时添加。"

## 代码修改清单

### State对象精简
```typescript
// 删除
- isEditing: false
- mapLayout: {}
- elementsSidebar: false
- containers: {}
- isContainerMode: false
- selectedContainer: null

// 保留
- isMapOpen: boolean
- cachedData: { mapLocations, mapElements, ... }
```

### 常量精简
```typescript
// 删除
- STORAGE_MAP_LAYOUT_KEY
- STORAGE_CONTAINERS_KEY

// 保留
- MAP_TABLE_MARKER = '[浮岛地图]'
```

### 函数删除列表 (约30个)
```
❌ renderElementsSidebar()
❌ saveMapLayout()
❌ isContainerElement()
❌ getContainerItems()
❌ addItemToContainer()
❌ removeItemFromContainer()
❌ removeItemFromAllContainers()
❌ isItemInAnyContainer()
❌ getItemContainer()
❌ saveContainers()
❌ clearContainer()
❌ cleanupContainers()
... 等等
```

### CSS删除 (约300行)
```scss
❌ .ci-sidebar-* (侧边栏所有样式)
❌ .ci-element-card (元素卡片)
❌ .ci-location-group (地点分组)
❌ .resize-handle (调整尺寸拖拽点)
❌ .draggable 相关样式
```

## renderMap简化对比

### 旧版本 (复杂)
```typescript
function renderMap() {
  // 1. 清理容器数据
  cleanupContainers();
  
  // 2. 地点循环
  adjustedLocs.forEach(l => {
    // 地点渲染
    // 编辑模式判断
    if (state.isEditing) {
      // 添加拖拽点
      // 绑定调整尺寸
      // 绑定拖拽移动
    }
    
    // 收集元素
    const locElements = filter + hasLayout check
    
    // 渲染元素
    locElements.forEach(el => {
      if (state.mapLayout[el.name]) {
        // 使用保存的位置
      }
      if (state.isEditing) {
        // 绑定拖拽
      }
    });
  });
  
  // 3. 独立元素循环
  Object.keys(mapLayout).forEach...
  
  // 4. 碰撞检测
  if (!state.isEditing) {
    resolveElementCollisions();
  }
}
```

### 新版本 (简洁)
```typescript
function renderMap() {
  // 1. 计算viewBox
  // 2. 地点循环
  adjustedLocs.forEach(loc => {
    // 渲染地点矩形+文字
    // 点击事件: showMapPopup(['前往XX'])
    
    // 渲染地点内元素
    locElements.forEach((el, i) => {
      const x = loc.x + pseudoRandom(i) * loc.width
      const y = loc.y + pseudoRandom(i) * loc.height
      renderElement(el, x, y)
    });
    
    // 渲染主角标记
    if (isProtagonistHere) {
      renderProtagonistMarker(x, y)
    }
  });
}

function renderElement(el, x, y) {
  // 矩形 + 文字
  // 点击事件: showMapPopup(interactions)
}
```

## 文件大小对比

| 版本 | 行数 | 大小 | 功能数 |
|------|------|------|--------|
| v8.7 | 3373行 | ~120KB | 60+ |
| v8.8 | ~2500行 | ~80KB | 30+ |
| 减少 | -873行 | -40KB | -30 |

## 迁移步骤

1. **备份当前版本**
   ```
   cp index.ts index_v8.7_backup.ts
   ```

2. **删除state字段**
   ```typescript
   // 删除 state 中的:
   - isEditing
   - mapLayout
   - elementsSidebar
   - containers
   - isContainerMode  
   - selectedContainer
   ```

3. **删除常量**
   ```typescript
   // 删除:
   - STORAGE_MAP_LAYOUT_KEY
   - STORAGE_CONTAINERS_KEY
   ```

4. **删除函数**
   - 所有容器管理函数(10个)
   - renderElementsSidebar
   - saveMapLayout
   - 编辑相关辅助函数

5. **简化renderMap**
   - 删除编辑模式判断
   - 删除mapLayout读取
   - 使用伪随机位置
   - 删除拖拽绑定

6. **删除CSS**
   - 侧边栏样式(200行)
   - 拖拽点样式
   - 编辑模式样式

7. **修改injectMapTables**
   - 使用标准表格模板
   - 恢复标准SourceData

8. **删除UI元素**
   - 删除"编辑"按钮
   - 删除侧边栏HTML

## 测试清单

### 基础显示
- [ ] 地图正常显示
- [ ] 地点有矩形和文字
- [ ] 元素自动分布在地点内
- [ ] 主角位置有脉冲标记

### 互动功能
- [ ] 点击地点显示"前往XX"
- [ ] 点击元素显示互动选项
- [ ] 点击选项发送到输入框

### 地图操作
- [ ] 鼠标拖拽可以平移
- [ ] 滚轮可以缩放
- [ ] +/- 按钮缩放正常

### 数据库
- [ ] 注入的表格是标准模板
- [ ] sourceData 是标准提示词
- [ ] 列名正确

## 优势

### 1. 更简单
- 代码减少30%
- 功能聚焦核心
- 易于维护

### 2. 更稳定
- 删除复杂的状态管理
- 删除localStorage依赖
- 减少bug可能性

### 3. 更快速
- 渲染更快(无编辑模式判断)
- 无需保存/加载布局
- 减少DOM操作

### 4. 更符合原始设计
- AI自动布局地图
- 用户只需浏览和互动
- 不需要手动调整

## 用户体验

**旧版本(v8.7)**:
```
用户: "这地图怎么编辑?"
系统: "点编辑按钮,然后拖元素,调尺寸..."
用户: "太复杂了,我不想编辑"
```

**新版本(v8.8)**:
```
用户: "地图在哪?"
系统: "点这里,自动显示"
用户: "很好,点击可以互动"
```

## 参照前端美化HTML

v8.8完全参照前端美化HTML:
- ✅ 只显示地点和元素
- ✅ 元素位置自动分布
- ✅ 点击显示互动选项
- ✅ 拖拽平移,滚轮缩放
- ✅ 简洁清爽,无复杂操作

## 下一步

需要我现在开始删除代码并重写吗?

这将是一个大规模的简化重构,预计需要:
1. 删除约1000行代码
2. 重写renderMap函数
3. 删除约300行CSS
4. 修改表格模板
5. 全面测试

预计时间: 1-2小时
