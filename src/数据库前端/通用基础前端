<details>
  <summary>📋数据界面</summary>
  ```html
  <!DOCTYPE html>
  <html lang="zh-CN">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
      <title>游戏世界数据面板</title>
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
      <link
        href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;700&family=Kaiti&display=swap"
        rel="stylesheet"
      />
      <style>
        :root {
          --bg-main: #fdfaf5; /* 仿羊皮纸背景色 */
          --bg-container: rgba(245, 235, 220, 0.8); /* 容器半透明背景 */
          --text-main: #5a3e2b; /* 主要文字颜色，深棕色 */
          --text-title: #3e271a; /* 标题颜色，更深的棕色 */
          --accent-color1: #8c6e54; /* 点缀色1，用于按钮和边框 */
          --accent-color2: #b48a5c; /* 点缀色2，用于高亮和交互 */
          --border-color: rgba(90, 62, 43, 0.3); /* 边框颜色 */
          --shadow-color: rgba(62, 39, 26, 0.2);
          --font-title: 'Noto Serif SC', serif;
          --font-main: 'Kaiti', 'Noto Serif SC', serif;
          --radius-base: 8px;
        }

        /* 夜间模式变量 */
        .night-mode {
          --bg-main: #2d2d2d;
          --bg-container: rgba(45, 45, 45, 0.9);
          --text-main: #dbdbd6;
          --text-title: #dbdbd6;
          --accent-color1: #8479b8;
          --accent-color2: #8479b8;
          --border-color: #444;
          --shadow-color: rgba(0, 0, 0, 0.4);
        }

        body {
          background-color: transparent;
          margin: 0;
          padding: 15px;
          font-family: var(--font-main);
          color: var(--text-main);
          font-size: 17px; /* 稍微增大基础字号 */
          line-height: 1.6; /* 改善行间距 */
          background-image: url('data:image/jpeg;base64,'); /* 模拟纸张纹理 */
          transition: all 0.3s ease;
        }

        .night-mode body {
          background-image: none;
        }

        .map-interface {
          width: 100%;
          margin: 0 auto;
          background-color: var(--bg-main);
          border: 1px solid var(--border-color); /* 边框变细 */
          border-radius: var(--radius-base);
          padding: 20px;
          box-sizing: border-box;
          box-shadow: 0 4px 12px var(--shadow-color); /* 阴影更柔和 */
          display: flex;
          flex-direction: column;
          gap: 10px; /* 进一步减小主要模块间距 */
          transition: all 0.3s ease;
        }

        .top-bar {
          display: flex;
          justify-content: space-between; /* 改为两端对齐 */
          align-items: center; /* 垂直居中 */
          padding-bottom: 15px;
          border-bottom: 1px solid var(--border-color);
          flex-wrap: wrap;
          gap: 10px;
        }

        .title-container h1 {
          font-family: var(--font-title);
          color: var(--text-title);
          font-size: 2rem; /* 增大标题 */
          margin: 0 0 5px 0; /* 调整外边距 */
          font-weight: 700;
          text-shadow: 1px 1px 2px var(--shadow-color);
        }
        .current-time-container {
          font-size: 0.95rem; /* 协调字体大小 */
          color: var(--text-main);
          opacity: 0.9;
          display: flex;
          align-items: center;
          justify-content: center; /* 确保内部也居中 */
          gap: 8px;
          white-space: nowrap;
        }

        /* 夜间模式切换按钮 */
        .night-mode-toggle {
          background: transparent !important;
          border: 1px solid var(--accent-color1) !important;
          color: var(--text-main) !important;
          border-radius: 4px !important;
          padding: 6px 12px !important;
          font-size: 12px !important;
          cursor: pointer !important;
          font-family: 'Microsoft YaHei', sans-serif !important;
          transition: all 0.3s ease !important;
          margin-left: 8px !important;
        }
        .night-mode-toggle:hover {
          background: var(--accent-color1) !important;
          color: white !important;
          transform: translateY(-1px) !important;
        }

        /* 移除旧的按钮导航栏，因为按钮将被移到新的状态栏中 */
        .main-nav-buttons {
          display: none;
        }

        /* --- 新增：主角状态栏 --- */
        #protagonist-status-bar {
          display: flex;
          gap: 15px; /* 减小头像和信息区的间距 */
          align-items: flex-start; /* 顶部对齐 */
          padding: 10px; /* 减小内边距 */
          background: var(--bg-container);
          border: 1px solid var(--border-color);
          border-radius: var(--radius-base);
          margin: 0; /* 移除外边距 */
          box-shadow: 0 2px 8px var(--shadow-color);
          transition: all 0.3s ease;
        }
        .protagonist-portrait {
          width: 80px; /* 缩小头像尺寸 */
          height: 80px;
          border-radius: var(--radius-base);
          border: 2px solid var(--accent-color1);
          overflow: hidden;
          flex-shrink: 0;
          cursor: pointer;
          position: relative;
          background-color: #e0d5c8;
          box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15); /* 添加轻微阴影 */
          transition: all 0.3s ease;
        }
        .night-mode .protagonist-portrait {
          background-color: #3a3a3a;
          border-color: var(--accent-color1);
        }
        .protagonist-portrait img {
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
        /* 头像上传提示 */
        .portrait-upload-prompt {
          position: absolute;
          bottom: 0;
          left: 0;
          right: 0;
          background: rgba(0, 0, 0, 0.6);
          color: white;
          text-align: center;
          font-size: 12px;
          padding: 4px 0;
          opacity: 0;
          transition: opacity 0.3s ease;
        }
        .protagonist-portrait:hover .portrait-upload-prompt {
          opacity: 1;
        }
        .protagonist-core-info {
          flex-grow: 1;
          display: flex;
          flex-direction: row; /* 水平排列 */
          justify-content: space-between; /* 两端对齐 */
          align-items: center; /* 改为垂直居中 */
          gap: 10px;
        }
        .protagonist-text-info {
          display: flex;
          flex-direction: column;
          gap: 4px; /* 文本信息内部间距 */
          align-items: flex-start; /* 左对齐 */
        }
        .protagonist-name-info {
          display: flex;
          flex-direction: column; /* 垂直排列名字和详情 */
          gap: 4px; /* 名字和详情的间距 */
          align-items: flex-start; /* 左对齐 */
        }
        #protagonist-name {
          font-family: var(--font-title);
          font-size: 1.5rem; /* 恢复稍大的字号 */
          color: var(--text-title);
          font-weight: 700;
          line-height: 1.2; /* 调整行高使之更紧凑 */
        }
        .protagonist-details {
          display: flex;
          flex-direction: column;
          gap: 2px; /* 行间距 */
        }
        .protagonist-details .detail-item {
          font-size: 0.9rem;
          opacity: 0.9;
        }
        .protagonist-money {
          font-weight: bold;
          color: var(--text-title);
        }
        .protagonist-main-controls {
          display: flex;
          gap: 8px; /* 缩小按钮间距 */
          flex-wrap: nowrap; /* 不换行 */
          align-items: center; /* 垂直居中 */
        }
        .protagonist-stats-container {
          flex-shrink: 0;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          min-width: 120px; /* 调整宽度 */
          padding-left: 10px; /* 减小左边距 */
          border-left: 1px solid var(--border-color); /* 添加分割线 */
        }

        .function-btn {
          background: var(--bg-container);
          border: 1px solid var(--border-color);
          color: var(--text-title);
          padding: 12px 20px; /* 增大按钮内边距 */
          border-radius: var(--radius-base);
          font-size: 1.1rem; /* 增大字体 */
          cursor: pointer;
          transition: all 0.2s ease-in-out; /* 调整过渡效果 */
          font-family: var(--font-title);
          box-shadow: 0 2px 5px var(--shadow-color);
          text-shadow: 1px 1px 1px rgba(255, 255, 255, 0.2); /* 增加文字质感 */
        }
        .function-btn:hover {
          background-color: var(--accent-color1);
          color: var(--bg-main);
          box-shadow: 0 4px 8px var(--shadow-color);
          transform: translateY(-2px);
        }
        .function-btn i {
          margin-right: 8px;
        }

        .horizontal-scroll-list .function-btn {
          flex-shrink: 0; /* 防止按钮在flex容器中被压缩 */
          white-space: nowrap; /* 防止按钮内文字换行 */
        }

        /* --- 主角状态栏圆形按钮样式 --- */
        .protagonist-main-controls .function-btn {
          width: 44px; /* 固定宽度 */
          height: 44px; /* 固定高度 */
          border-radius: 50%; /* 圆形 */
          padding: 0; /* 移除内边距 */
          font-size: 1.2rem; /* 调整图标大小 */
          display: flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
        }
        .protagonist-main-controls .function-btn span {
          display: none; /* 隐藏文字 */
        }
        .protagonist-main-controls .function-btn i {
          margin-right: 0; /* 移除图标的边距 */
        }

        .content-grid {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }

        .grid-item {
          background: var(--bg-container);
          border: 1px solid var(--border-color);
          border-radius: var(--radius-base);
          padding: 25px; /* 增加内边距 */
          box-shadow: 0 2px 8px var(--shadow-color);
          flex-grow: 1;
          min-width: 300px;
          transition: all 0.3s ease;
        }

        #protagonist-section {
          grid-column: 1 / -1;
        } /* 主角部分占满整行 */

        .section-title {
          font-family: var(--font-title);
          font-size: 1.4rem; /* 减小标题字号 */
          color: var(--text-title);
          margin-bottom: 15px;
          padding-bottom: 10px;
          border-bottom: 1px solid var(--border-color);
          display: flex;
          align-items: center;
          font-weight: 700; /* 加粗 */
          transition: all 0.3s ease;
        }
        .section-title i {
          margin-right: 12px;
          color: var(--accent-color1);
          font-size: 1.1em; /* 稍微放大图标 */
          vertical-align: -2px; /* 调整图标垂直对齐 */
        }

        /* Protagonist special UI */
        .protagonist-layout {
          display: flex;
          gap: 20px;
          flex-wrap: wrap;
        }
        .protagonist-info {
          flex: 2;
          min-width: 250px;
        }
        .protagonist-stats {
          flex: 1;
          min-width: 200px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
        }

        .info-grid {
          display: grid;
          grid-template-columns: auto 1fr;
          gap: 10px;
          align-items: center;
        }
        .info-grid dt {
          font-weight: bold;
          color: var(--text-title);
        }
        .info-grid dd {
          margin: 0;
          white-space: pre-wrap;
        }

        /* Key Characters List */
        .character-card {
          background: rgba(255, 255, 255, 0.5);
          padding: 15px;
          border-radius: var(--radius-base);
          margin-bottom: 10px;
          border-left: 5px solid var(--accent-color2);
          cursor: pointer;
          transition: all 0.2s ease;
        }
        .night-mode .character-card {
          background: rgba(58, 58, 58, 0.5);
        }
        .character-card:hover {
          transform: translateX(5px);
          background: rgba(255, 255, 255, 0.8);
        }
        .night-mode .character-card:hover {
          background: rgba(58, 58, 58, 0.8);
        }
        .character-header {
          display: flex;
          justify-content: space-between;
          align-items: center;
        }
        .character-name {
          font-family: var(--font-title);
          font-size: 1.2rem;
          color: var(--text-title);
        }
        .character-status {
          font-size: 0.9rem;
          font-style: italic;
        }

        /* Generic List Style for Skills, Inventory, etc. */
        .item-list {
          list-style: none;
          padding: 0;
          margin: 0;
        }
        .list-item {
          padding: 12px 0;
          border-bottom: 1px dashed var(--border-color);
          display: flex;
          justify-content: space-between;
          align-items: center;
          transition: all 0.3s ease;
        }
        .list-item:last-child {
          border-bottom: none;
        }
        .item-name {
          font-weight: bold;
        }
        .item-details {
          font-size: 0.9rem;
          color: var(--text-main);
        }

        /* Quest Log Specific Styles */
        #quest-items-container .list-item {
          display: block;
          padding: 0;
          border-bottom: none;
        }
        #quest-items-container .list-item:last-child {
          margin-bottom: 0;
        }
        .quest-item {
          background-color: rgba(255, 255, 255, 0.4);
          border: 1px solid var(--border-color);
          border-left: 4px solid var(--accent-color2);
          padding: 12px 15px;
          margin-bottom: 10px;
          border-radius: var(--radius-base);
          transition: background-color 0.2s ease;
        }
        .night-mode .quest-item {
          background-color: rgba(58, 58, 58, 0.4);
        }
        .quest-item:hover {
          background-color: rgba(255, 255, 255, 0.7);
        }
        .night-mode .quest-item:hover {
          background-color: rgba(58, 58, 58, 0.7);
        }
        .quest-item .item-name {
          font-family: var(--font-title);
          font-size: 1.15rem;
          margin-bottom: 8px;
          color: var(--text-title);
          font-weight: bold;
        }
        .quest-item .item-details {
          font-size: 0.9rem;
          margin-bottom: 4px;
          padding-left: 5px;
        }
        .quest-item .item-details b {
          color: var(--text-title);
          margin-right: 5px;
        }

        .modal-overlay {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background-color: rgba(0, 0, 0, 0.7);
          backdrop-filter: blur(5px);
          z-index: 1000;
          display: flex;
          align-items: center;
          justify-content: center;
          opacity: 0;
          pointer-events: none;
          transition: opacity 0.3s ease;
        }
        .night-mode .modal-overlay {
          background-color: rgba(0, 0, 0, 0.8);
        }
        .modal-overlay.visible {
          opacity: 1;
          pointer-events: auto;
        }
        .modal-dialog {
          background: var(--bg-main);
          border: 2px solid var(--accent-color1);
          border-radius: var(--radius-base);
          padding: 25px; /* 调整内边距 */
          width: 90%;
          max-width: 600px;
          position: relative;
          box-shadow: 0 5px 20px var(--shadow-color);
          max-height: 85vh; /* 限制最大高度 */
          display: flex;
          flex-direction: column;
          transition: all 0.3s ease;
        }
        .modal-dialog h4 {
          font-family: var(--font-title);
          font-size: 1.6rem;
          color: var(--text-title);
          text-align: center;
          margin-top: 0;
          margin-bottom: 20px;
        }
        .close-modal-button {
          position: absolute;
          top: 15px;
          right: 15px;
          background: none;
          border: none;
          font-size: 28px;
          color: var(--accent-color1);
          cursor: pointer;
          transition: color 0.2s ease;
        }
        .close-modal-button:hover {
          color: var(--text-title);
        }
        .modal-content {
          overflow-y: auto; /* 内容溢出时显示滚动条 */
          flex-grow: 1;
          padding-right: 15px;
          word-wrap: break-word; /* 允许长单词或URL换行 */
        }
        .modal-content::-webkit-scrollbar {
          width: 6px;
        }
        .modal-content::-webkit-scrollbar-track {
          background: var(--bg-container);
        }
        .modal-content::-webkit-scrollbar-thumb {
          background: var(--accent-color1);
          border-radius: 3px;
        }

        .interaction-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
          gap: 10px;
          margin-top: 20px;
          padding-top: 20px;
          border-top: 1px solid var(--border-color);
        }
        /* 仅针对角色弹窗，强制2x2网格 */
        #character-modal .interaction-grid {
          grid-template-columns: repeat(2, 1fr);
        }
        .interaction-grid .function-btn {
          width: 100%;
          padding: 12px 10px; /* 调整内边距 */
        }

        .list-item-content {
          flex-grow: 1;
          margin-right: 10px;
        }
        .use-button {
          background: var(--accent-color2);
          color: white;
          border: none;
          padding: 5px 12px;
          border-radius: var(--radius-base);
          cursor: pointer;
          font-size: 0.9rem;
          margin-left: 15px;
          flex-shrink: 0;
          transition: background-color 0.2s;
        }
        .use-button:hover {
          background: var(--accent-color1);
        }

        .horizontal-scroll-list {
          display: flex;
          flex-wrap: nowrap;
          gap: 12px; /* 增加按钮间距 */
          overflow-x: auto;
          padding: 5px 0 15px 0; /* 调整内边距 */
          align-items: center;
        }
        .horizontal-scroll-list::-webkit-scrollbar {
          height: 6px;
        } /* 加粗滚动条 */
        .horizontal-scroll-list::-webkit-scrollbar-track {
          background: rgba(0, 0, 0, 0.1);
        }
        .night-mode .horizontal-scroll-list::-webkit-scrollbar-track {
          background: rgba(255, 255, 255, 0.1);
        }
        .horizontal-scroll-list::-webkit-scrollbar-thumb {
          background: var(--accent-color1);
          border-radius: 2px;
        }

        /* 移除所有宽屏样式 */

        @media (max-width: 768px) {
          :root {
            --font-size-base: 12px;
            --spacing-md: 10px;
            --spacing-lg: 15px;
          }
          body {
            padding: 5px;
          }
          .map-interface {
            padding: var(--spacing-lg) var(--spacing-md);
          }
          .partner-button-list {
            display: flex;
            flex-wrap: nowrap;
            overflow-x: auto;
            padding-bottom: 10px;
            -webkit-overflow-scrolling: touch;
            scrollbar-width: none;
            flex-shrink: 0;
            white-space: nowrap;
          }
          .partner-button-list::-webkit-scrollbar {
            display: none;
          }
          .partner-button {
            padding: 8px 14px;
            font-size: 0.9rem;
            flex-shrink: 0;
            white-space: nowrap;
          }
          #important-character-buttons .partner-button {
            padding: 5px 10px 5px 5px;
          }
          .sub-location-grid {
            grid-template-columns: 1fr;
          }
          .modal-dialog {
            width: 95vw;
            padding: var(--spacing-lg) 15px;
          }
          .modal-dialog h4 {
            font-size: 1.3rem;
          }
          #modal-character-name {
            font-size: 1.7rem;
          }
          .attributes-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: var(--spacing-sm);
          }
          .attribute-item {
            padding: var(--spacing-xs);
          }
          .interaction-options-grid {
            grid-template-columns: 1fr;
            gap: var(--spacing-sm);
          }
          .interaction-button,
          .sub-location-button {
            padding: 10px;
            font-size: 0.95rem;
          }
          .shop-item,
          .inventory-item,
          .skill-item {
            flex-direction: column;
            align-items: center;
            gap: 10px;
          }
          .shop-item > div,
          .inventory-item > div,
          .skill-item > div {
            text-align: center;
          }
          .use-button {
            width: auto;
            padding-left: 40px;
            padding-right: 40px;
          }
        }

        @media (max-width: 480px) {
          #protagonist-status-bar {
            flex-wrap: wrap; /* 允许换行 */
            gap: 10px;
            justify-content: center; /* 居中头像等元素 */
          }

          .protagonist-portrait {
            width: 60px; /* 进一步缩小头像 */
            height: 60px;
          }

          .protagonist-stats-container {
            padding-left: 0;
            border-left: none; /* 移除分割线 */
            width: 100%; /* 占满可用空间以帮助居中 */
            justify-content: center; /* 水平居中 */
            align-items: center; /* 垂直居中 */
          }

          .protagonist-core-info {
            flex-direction: column; /* 改为垂直排列 */
            align-items: stretch; /* 拉伸以填充宽度 */
            gap: 12px;
            width: 100%; /* 占满整行宽度 */
          }

          .protagonist-text-info {
            align-items: center; /* 居中对齐文本 */
            text-align: center;
            width: 100%;
          }
          .protagonist-name-info,
          #protagonist-name {
            align-items: center; /* 确保名字也居中 */
          }
          #protagonist-name {
            font-size: 1.3rem; /* 调整名字大小 */
          }

          .protagonist-main-controls {
            order: -1; /* 将按钮提到最前面 */
            justify-content: center; /* 居中按钮 */
            width: 100%;
          }
        }
      </style>
    </head>
    <body>
      <div class="map-interface">
        <!-- 顶部信息与导航栏 -->
        <header class="top-bar">
          <div class="title-container">
            <h1 id="location-title">地点加载中...</h1>
            <div id="current-time-container" class="current-time-container">
              <i class="fas fa-clock"></i>
              <span id="current-time">----</span>
            </div>
          </div>
          <button class="night-mode-toggle" id="night-mode-toggle" title="切换夜间模式">🌙</button>
        </header>

        <!-- 新增：主角状态栏 -->
        <section id="protagonist-status-bar">
          <div id="protagonist-portrait-container" class="protagonist-portrait" title="点击上传自定义头像">
            <img id="protagonist-portrait-img" src="" alt="主角头像" />
            <div class="portrait-upload-prompt">更换头像</div>
          </div>
          <div class="protagonist-core-info">
            <div class="protagonist-text-info">
              <div class="protagonist-name-info">
                <h2 id="protagonist-name" style="margin: 0"></h2>
                <div id="protagonist-details" class="protagonist-details"></div>
              </div>
            </div>
            <div class="protagonist-main-controls">
              <!-- 详细信息按钮已移除，功能按钮保留 -->
              <div class="function-btn" id="skill-button"><i class="fas fa-magic"></i><span>技能</span></div>
              <div class="function-btn" id="inventory-button"><i class="fas fa-box-open"></i><span>背包</span></div>
              <div class="function-btn" id="quest-button"><i class="fas fa-scroll"></i><span>任务</span></div>
            </div>
          </div>
        </section>

        <!-- 重要角色栏 -->
        <section id="key-characters-section">
          <div class="section-title"><i class="fas fa-users"></i>重要角色信息</div>
          <div id="character-bar" class="horizontal-scroll-list">
            <!-- Character buttons will be inserted here by JS -->
          </div>
        </section>

        <!-- 主角互动选项 -->
        <section id="protagonist-interactions-section">
          <div class="section-title"><i class="fas fa-tasks"></i>可用行动</div>
          <div id="protagonist-interactions-container" class="interaction-grid">
            <!-- Protagonist interaction buttons will be inserted here by JS -->
          </div>
        </section>

        <!-- 主内容区 -->
        <main class="content-grid" style="margin-top: 20px">
          <!-- Future content can be added here -->
        </main>
      </div>

      <!-- 弹窗 -->
      <div id="character-modal" class="modal-overlay">
        <div class="modal-dialog">
          <button class="close-modal-button">&times;</button>
          <h4 id="modal-character-name"></h4>
          <div id="modal-character-content" class="modal-content"></div>
          <div id="modal-character-interactions" class="interaction-grid"></div>
        </div>
      </div>
      <!-- 其他弹窗 (技能、背包等) -->
      <div id="skill-modal" class="modal-overlay">
        <div class="modal-dialog">
          <button class="close-modal-button">&times;</button>
          <h4><i class="fas fa-magic"></i> 技能列表</h4>
          <div id="skill-items-container" class="modal-content"></div>
        </div>
      </div>
      <div id="inventory-modal" class="modal-overlay">
        <div class="modal-dialog">
          <button class="close-modal-button">&times;</button>
          <h4><i class="fas fa-box-open"></i> 背包</h4>
          <div id="inventory-items-container" class="modal-content"></div>
        </div>
      </div>
      <div id="quest-modal" class="modal-overlay">
        <div class="modal-dialog">
          <button class="close-modal-button">&times;</button>
          <h4><i class="fas fa-scroll"></i> 任务日志</h4>
          <div id="quest-items-container" class="modal-content"></div>
        </div>
      </div>
      <div id="shop-modal" class="modal-overlay">
        <div class="modal-dialog">
          <button class="close-modal-button">&times;</button>
          <h4><i class="fas fa-store"></i> 商店</h4>
          <div id="shop-items-container" class="modal-content"></div>
        </div>
      </div>

      <script>
        // 夜间模式相关功能
        const STORAGE_KEY_NIGHT_MODE = 'frontend_night_mode';

        // 获取夜间模式状态
        function getNightModeState() {
          try {
            const saved = localStorage.getItem(STORAGE_KEY_NIGHT_MODE);
            return saved ? JSON.parse(saved) : false;
          } catch (e) {
            return false;
          }
        }

        // 保存夜间模式状态
        function saveNightModeState(enabled) {
          try {
            localStorage.setItem(STORAGE_KEY_NIGHT_MODE, JSON.stringify(enabled));
          } catch (e) {
            console.error('保存夜间模式状态失败:', e);
          }
        }

        // 切换夜间模式
        function toggleNightMode() {
          const body = document.body;
          const isNightMode = body.classList.contains('night-mode');
          const toggleBtn = document.getElementById('night-mode-toggle');

          if (isNightMode) {
            body.classList.remove('night-mode');
            toggleBtn.textContent = '🌙';
            saveNightModeState(false);
          } else {
            body.classList.add('night-mode');
            toggleBtn.textContent = '☀️';
            saveNightModeState(true);
          }
        }

        // 应用夜间模式状态
        function applyNightModeState() {
          const isNightMode = getNightModeState();
          const body = document.body;
          const toggleBtn = document.getElementById('night-mode-toggle');

          if (isNightMode) {
            body.classList.add('night-mode');
            toggleBtn.textContent = '☀️';
          } else {
            body.classList.remove('night-mode');
            toggleBtn.textContent = '🌙';
          }
        }

        document.addEventListener('DOMContentLoaded', function () {
          // 初始化夜间模式
          applyNightModeState();

          // 绑定夜间模式切换事件
          document.getElementById('night-mode-toggle').addEventListener('click', toggleNightMode);

          // Use live data source from parent, or a fallback that returns nothing.
          const dataSourceAPI =
            typeof parent !== 'undefined' && parent.AutoCardUpdaterAPI
              ? parent.AutoCardUpdaterAPI
              : {
                  exportTableAsJson: function () {
                    console.log('No data source found.');
                    return {}; // Return empty object if no source
                  },
                  registerTableUpdateCallback: function (callback) {
                    console.log('No data source found to register callback.');
                  },
                };

          function getTableData() {
            try {
              return dataSourceAPI.exportTableAsJson();
            } catch (e) {
              console.error('Error getting table data:', e);
              return {};
            }
          }

          function processJsonData(json) {
            const tables = {};
            if (!json || typeof json !== 'object') {
              console.error('Invalid JSON data provided.');
              return null;
            }
            for (const sheetId in json) {
              if (Object.prototype.hasOwnProperty.call(json, sheetId) && json[sheetId] && json[sheetId].name) {
                const sheet = json[sheetId];
                tables[sheet.name] = {
                  headers: sheet.content[0] || [],
                  rows: sheet.content.slice(1),
                };
              }
            }
            return tables;
          }

          function renderUI(tables) {
            if (!tables) return;

            // --- 1. 渲染全局数据 ---
            const globalTable = tables['全局数据表'];
            const globalData = globalTable?.rows[0] || [];
            const globalHeaders = globalTable?.headers || [];

            document.getElementById('location-title').textContent =
              globalData[globalHeaders.indexOf('主角当前所在地点')] || '未知地点';
            document.getElementById('current-time').textContent =
              `当前时间：${globalData[globalHeaders.indexOf('当前时间')] || '----'}`;

            // --- 2. 渲染主角状态栏 ---
            const pInfoTable = tables['主角信息'];
            const pInfoHeaders = pInfoTable?.headers || [];
            const pInfo = pInfoTable?.rows[0] || [];

            // 总是尝试加载头像
            const charactersTable = tables['重要人物表'];
            let protagonistAvatarUrl = '';
            if (pInfo.length > 0) {
              const protagonistName = pInfo[pInfoHeaders.indexOf('人物名称')];
              if (charactersTable && charactersTable.rows) {
                const protagonistEntry = charactersTable.rows.find(
                  row => row[charactersTable.headers.indexOf('姓名')] === protagonistName,
                );
                if (protagonistEntry) {
                  protagonistAvatarUrl = protagonistEntry[charactersTable.headers.indexOf('头像')] || '';
                }
              }
            }
            loadProtagonistAvatar(protagonistAvatarUrl, '主角');

            if (pInfo.length > 0) {
              // 基本信息
              document.getElementById('protagonist-name').textContent =
                pInfo[pInfoHeaders.indexOf('人物名称')] || '主角';
              const detailsContainer = document.getElementById('protagonist-details');
              detailsContainer.innerHTML = `
                        <div class="detail-item">${pInfo[pInfoHeaders.indexOf('性别/年龄')] || '未知'}</div>
                        <div class="detail-item">${pInfo[pInfoHeaders.indexOf('职业/身份')] || '未知'}</div>
                    `;

              // 渲染主角互动选项
              const interactionsContainer = document.getElementById('protagonist-interactions-container');
              interactionsContainer.innerHTML = '';
              const interactionHeaders = ['选项一', '选项二', '选项三', '选项四'];
              interactionHeaders.forEach(header => {
                const actionText = pInfo[pInfoHeaders.indexOf(header)];
                if (actionText) {
                  const btn = document.createElement('button');
                  btn.className = 'function-btn';
                  btn.textContent = actionText;
                  btn.onclick = () => sendGameActionRequest(actionText);
                  interactionsContainer.appendChild(btn);
                }
              });
            }

            // --- 3. 渲染重要人物按钮 (包括主角) ---
            const characterBar = document.getElementById('character-bar');
            characterBar.innerHTML = '';

            // 首先添加主角按钮
            if (pInfo.length > 0) {
              const pBtn = document.createElement('button');
              pBtn.className = 'function-btn';
              pBtn.innerHTML = `<i class="fas fa-user-circle"></i> ${pInfo[pInfoHeaders.indexOf('人物名称')] || '主角'}`;
              pBtn.onclick = () =>
                openCharacterModal({
                  isProtagonist: true,
                  info: pInfo,
                  infoHeaders: pInfoHeaders,
                });
              characterBar.appendChild(pBtn);
            }

            const characters = tables['重要人物表']?.rows || [];
            const charHeaders = tables['重要人物表']?.headers || [];
            const protagonistName = pInfo[pInfoHeaders.indexOf('人物名称')];
            characters.forEach(charData => {
              const charName = charData[charHeaders.indexOf('姓名')];
              // 过滤掉主角本人，因为已在上面独立添加
              if (charName && charName !== protagonistName) {
                const isDeparted = charData[charHeaders.indexOf('是否离场')] === '是';
                const charBtn = document.createElement('button');
                charBtn.className = 'function-btn';
                charBtn.innerHTML = `<i class="fas fa-id-badge"></i> ${charName}`;
                charBtn.onclick = () => openCharacterModal({ info: charData, infoHeaders: charHeaders });
                if (isDeparted) {
                  charBtn.style.display = 'none';
                }
                characterBar.appendChild(charBtn);
              }
            });

            // --- 4. 设置弹窗内容 ---
            setupModalContent('skill-modal', 'skill-items-container', tables['主角技能表'], (item, headers) => {
              const listItem = document.createElement('li');
              listItem.className = 'list-item';

              const content = document.createElement('div');
              content.className = 'list-item-content';
              const skillName = item[headers.indexOf('技能名称')];
              content.innerHTML = `
                        <div class="item-name">${skillName} <small>[${item[headers.indexOf('等级/阶段')]}]</small></div>
                        <div class="item-details">${item[headers.indexOf('效果描述')]}</div>
                    `;

              const useButton = document.createElement('button');
              useButton.className = 'use-button';
              useButton.textContent = '使用';
              useButton.onclick = () => sendGameActionRequest(`使用技能 ${skillName}`);

              listItem.appendChild(content);
              listItem.appendChild(useButton);
              return listItem;
            });
            setupModalContent('inventory-modal', 'inventory-items-container', tables['背包物品表'], (item, headers) => {
              const listItem = document.createElement('li');
              listItem.className = 'list-item';

              const content = document.createElement('div');
              content.className = 'list-item-content';
              const itemName = item[headers.indexOf('物品名称')];
              content.innerHTML = `
                        <div class="item-name">${itemName} (x${item[headers.indexOf('数量')]})</div>
                        <div class="item-details">${item[headers.indexOf('描述/效果')]}</div>
                    `;

              const useButton = document.createElement('button');
              useButton.className = 'use-button';
              useButton.textContent = '使用';
              useButton.onclick = () => sendGameActionRequest(`使用物品 ${itemName}`);

              listItem.appendChild(content);
              listItem.appendChild(useButton);
              return listItem;
            });
            setupModalContent('quest-modal', 'quest-items-container', tables['任务与事件表'], (item, headers) => {
              const listItem = document.createElement('li');
              listItem.className = 'list-item';
              listItem.innerHTML = `
                        <div class="quest-item">
                            <div class="item-name">${item[headers.indexOf('任务名称')]} <small>[${item[headers.indexOf('任务类型')]}]</small></div>
                            <div class="item-details"><b>发布者:</b> ${item[headers.indexOf('发布者')]}</div>
                            <div class="item-details"><b>描述:</b> ${item[headers.indexOf('详细描述')]}</div>
                            <div class="item-details"><b>进度:</b> ${item[headers.indexOf('当前进度')]}</div>
                        </div>
                    `;
              return listItem;
            });
          }

          /**
           * 当图片加载失败时，应用一个备用UI（彩色背景加问号）。
           * @param {HTMLElement} element - 图片的容器元素。
           */
          function applyImageFallback(element) {
            if (!element) return;
            const colors = ['#5a5a5a', '#6a4c4c', '#4c6a4c', '#4c4c6a', '#6a6a4c', '#4c6a6a'];
            const randomColor = colors[Math.floor(Math.random() * colors.length)];

            element.innerHTML = ''; // 清除损坏的img标签
            element.style.backgroundImage = 'none';
            element.style.backgroundColor = randomColor;
            element.style.display = 'flex';
            element.style.justifyContent = 'center';
            element.style.alignItems = 'center';
            element.style.color = '#fff';
            const size = Math.min(element.offsetWidth, element.offsetHeight);
            element.style.fontSize = `${size * 0.6}px`;
            element.style.fontWeight = 'bold';
            element.style.lineHeight = '1';
            element.textContent = '?';
          }

          /**
           * 从多个来源加载主角头像，并提供强大的回退机制。
           * @param {string} avatarUrlFromData - 从数据源获取的头像URL。
           * @param {string} protagonistName - 主角名字，用于localStorage的键。
           */
          function loadProtagonistAvatar(avatarUrlFromData, protagonistName) {
            const container = document.getElementById('protagonist-portrait-container');
            const imgElement = document.getElementById('protagonist-portrait-img');
            const customAvatarKey = 'protagonist-custom-avatar-主角';
            const customAvatar = localStorage.getItem(customAvatarKey);
            const defaultAvatar =
              "data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ctext y='.9em' font-size='90'%3E👤%3C/text%3E%3C/svg%3E";

            // 清理旧的UI元素（如图标和重置按钮）
            const existingResetBtn = container.querySelector('.avatar-reset-btn');
            if (existingResetBtn) existingResetBtn.remove();

            // 确保图片和容器恢复正常样式
            imgElement.style.display = 'block';
            container.style.backgroundColor = '#e0d5c8';
            // 如果容器里有文字（比如fallback的问号），先清空
            if (container.textContent) container.textContent = '';
            // 确保图片元素在容器里
            if (!container.contains(imgElement)) container.appendChild(imgElement);

            const sources = [];
            if (customAvatar) sources.push(customAvatar);
            if (avatarUrlFromData && (avatarUrlFromData.startsWith('http') || avatarUrlFromData.startsWith('data:'))) {
              sources.push(avatarUrlFromData);
            }
            sources.push(defaultAvatar);

            let sourceIndex = 0;
            const tryNextSource = () => {
              if (sourceIndex >= sources.length) {
                applyImageFallback(container);
                return;
              }
              imgElement.src = sources[sourceIndex];
              sourceIndex++;
            };

            imgElement.onerror = tryNextSource;
            tryNextSource();
          }

          document.getElementById('protagonist-portrait-container').addEventListener('click', () => {
            const customAvatarKey = 'protagonist-custom-avatar-主角';

            const input = document.createElement('input');
            input.type = 'file';
            input.accept = 'image/*';
            input.onchange = event => {
              const file = event.target.files[0];
              if (!file) return;

              const reader = new FileReader();
              reader.onload = e => {
                const img = new Image();
                img.onload = () => {
                  const canvas = document.createElement('canvas');
                  const ctx = canvas.getContext('2d');
                  const TARGET_SIZE = 300;
                  canvas.width = TARGET_SIZE;
                  canvas.height = TARGET_SIZE;

                  // 裁剪为正方形
                  const size = Math.min(img.width, img.height);
                  const x = (img.width - size) / 2;
                  const y = (img.height - size) / 2;

                  ctx.drawImage(img, x, y, size, size, 0, 0, TARGET_SIZE, TARGET_SIZE);

                  const dataUrl = canvas.toDataURL('image/png');

                  try {
                    localStorage.setItem(customAvatarKey, dataUrl);
                    const imgElement = document.getElementById('protagonist-portrait-img');
                    imgElement.src = dataUrl;
                  } catch (err) {
                    console.error('保存自定义头像失败:', err);
                    alert('图片太大或浏览器缓存已满，无法保存。请尝试清理浏览器缓存或使用更小的图片。');
                  }
                };
                img.src = e.target.result;
              };
              reader.readAsDataURL(file);
            };
            input.click();
          });

          function openModal(modalId) {
            document.getElementById(modalId).classList.add('visible');
          }
          function closeModal(modalId) {
            document.getElementById(modalId).classList.remove('visible');
          }

          function openCharacterModal(character) {
            const modal = document.getElementById('character-modal');
            const nameEl = document.getElementById('modal-character-name');
            const contentContainer = document.getElementById('modal-character-content');
            const interactionsContainer = document.getElementById('modal-character-interactions');

            contentContainer.innerHTML = '';
            interactionsContainer.innerHTML = '';
            nameEl.textContent = character.isProtagonist
              ? character.info[character.infoHeaders.indexOf('人物名称')] || '主角'
              : character.info[character.infoHeaders.indexOf('姓名')] || '未知角色';

            const descFragment = document.createDocumentFragment();

            if (character.isProtagonist) {
              const details = [
                { label: '性别/年龄', value: character.info[character.infoHeaders.indexOf('性别/年龄')] },
                { label: '外貌', value: character.info[character.infoHeaders.indexOf('外貌特征')] },
                { label: '职业', value: character.info[character.infoHeaders.indexOf('职业/身份')] },
                { label: '经历', value: character.info[character.infoHeaders.indexOf('过往经历')] },
                { label: '性格', value: character.info[character.infoHeaders.indexOf('性格特点')] },
              ];
              details.forEach(detail => {
                if (detail.value) {
                  const p = document.createElement('p');
                  p.innerHTML = `<strong>${detail.label}:</strong> ${detail.value}`;
                  descFragment.appendChild(p);
                }
              });
            } else {
              const details = [
                { label: '性别/年龄', value: character.info[character.infoHeaders.indexOf('性别/年龄')] },
                { label: '外貌', value: character.info[character.infoHeaders.indexOf('外貌特征')] },
                { label: '性格', value: character.info[character.infoHeaders.indexOf('性格特点')] },
                { label: '持有物品', value: character.info[character.infoHeaders.indexOf('持有的重要物品')] },
                { label: '好感度', value: character.info[character.infoHeaders.indexOf('好感度')] },
                { label: '经历', value: character.info[character.infoHeaders.indexOf('过往经历')] },
              ];
              details.forEach(detail => {
                if (detail.value) {
                  const p = document.createElement('p');
                  p.innerHTML = `<strong>${detail.label}:</strong> ${detail.value}`;
                  descFragment.appendChild(p);
                }
              });
            }

            contentContainer.appendChild(descFragment);
            interactionsContainer.innerHTML = ''; //不再需要显示暂无互动选项了
            openModal('character-modal');
          }

          function setupModalContent(modalId, containerId, tableData, createItemElement) {
            const container = document.getElementById(containerId);
            container.innerHTML = '';
            if (!tableData || !tableData.rows || tableData.rows.length === 0) {
              container.innerHTML = '<p style="text-align:center;">暂无内容</p>';
              return;
            }
            const list = document.createElement('ul');
            list.className = 'item-list';
            tableData.rows.forEach(row => {
              const listItem = createItemElement(row, tableData.headers);
              list.appendChild(listItem);
            });
            container.appendChild(list);
          }

          function sendGameActionRequest(msg) {
            console.log('Action sent:', msg);
            if (typeof parent.triggerSlash === 'function') {
              try {
                parent.triggerSlash('/setinput ' + msg);
              } catch (e) {
                alert(e.toString());
              }
            } else {
              const input = parent.document.getElementById('send_textarea');
              if (input) {
                input.value = msg;
              } else {
                alert('错误: 无法找到输入框。');
              }
            }
          }

          // Initial Load
          const initialData = getTableData();
          const tables = processJsonData(initialData);
          renderUI(tables);

          // Register update callback
          dataSourceAPI.registerTableUpdateCallback(function (newData) {
            console.log('Received data update, refreshing UI...');
            const updatedTables = processJsonData(newData);
            renderUI(updatedTables);
          });

          // Bind button events
          document.getElementById('skill-button').onclick = () => openModal('skill-modal');
          document.getElementById('inventory-button').onclick = () => openModal('inventory-modal');
          document.getElementById('quest-button').onclick = () => openModal('quest-modal');
          // document.getElementById('shop-button').onclick = () => openModal('shop-modal');

          // Re-bind close for protagonist/character modal specifically, as it might be recreated
          const charModal = document.getElementById('character-modal');
          if (charModal) {
            charModal.querySelector('.close-modal-button').onclick = () => closeModal('character-modal');
          }

          // Bind close buttons for all modals
          document.querySelectorAll('.modal-overlay').forEach(modal => {
            modal.querySelector('.close-modal-button').onclick = () => modal.classList.remove('visible');
            // Also close on overlay click
            modal.addEventListener('click', function (event) {
              if (event.target === this) {
                this.classList.remove('visible');
              }
            });
          });
        });
      </script>
    </body>
  </html>

  ```
</details>
