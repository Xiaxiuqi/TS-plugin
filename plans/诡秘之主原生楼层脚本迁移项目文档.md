# 诡秘之主原生楼层脚本迁移项目文档

> 最后更新：2026-09-17  
> 状态：进行中  
> 工作目录：`public/guimizhizhu_ui/`

## 1. 目标

将 `数据库前端/regex-正文-诡秘之主catiger95-脚本分块/` 中的正则 `replaceString` 脚本，迁移为可由酒馆助手加载的模块化脚本项目。

迁移后的叙事时间线只能使用 SillyTavern 的真实聊天楼层：每次剧情行动必须产生一条真实 `user` 楼层和一条真实 `assistant` 楼层。前端输入框与酒馆原生输入框必须经过同一回合编排器，且不会重复发送。

## 2. 强制边界

- 不再劫持或替换酒馆原生聊天楼层。
- 不再使用 `chatHistoryCache`、`historyViewIndex`、私有快照、私有分支或时间线目录保存叙事历史。
- 不再写入每回合正文到 0 号消息；回合状态写入对应真实 assistant 楼层的 `data`。
- 删除默认长线记忆、核心记忆总结、精准记忆召回（RAG/API 1）及其设置、持久化、索引、API 调用和 UI。
- 保留与记忆无关的世界书、角色、战斗、抽卡、变量 AI、前端工具和“本周目经历”业务功能。
- 旧私有快照与用户既有世界书数据不主动删除；新版本只停止读取和写入它们。

## 3. 参考与来源

| 用途 | 位置 |
| --- | --- |
| 原始正则脚本 | `数据库前端/regex-正文-诡秘之主catiger95-脚本分块/` |
| 已有原生楼层迁移设计 | `.limcode/design/crypt-lord-native-chat-floor-migration.md` |
| 目标半成品 | `public/guimizhizhu_ui/` |
| 模块化加载参考 | `public/story_ui_lite_prod/` |
| MVU 约束 | `.cursor/rules/mvu变量框架.mdc` |

## 4. 目标项目结构

```text
public/guimizhizhu_ui/
  index.js
  loader.js
  shared/
    contract.js
    debug.js
    host-api.js
    lifecycle.js
  core/
    native-chat.js
    turn-orchestrator.js
    generation-bridge.js
    state-store.js
  prompts/
    context-builder.js
    native-history-policy.js
  modules/
    input-adapter/
    native-floor/
    game-shell/
    editor/
    floating-variable-editor/
    judgment-beautify/
    battle/
    tools-hub/
    manager-ui/
```

目录创建不等于功能完成。模块只有在注册、生命周期、业务路径和验证均完成后才能标记完成。

## 5. 回合模型

```text
前端 quick-send / 行动选项 / 酒馆原生输入
  -> input-adapter
  -> turn-orchestrator.submitNativeTurn(rawText, source)
  -> 创建真实 user 楼层
  -> generation-bridge 构建保留上下文与生成请求
  -> TavernHelper.generate
  -> 校验、解析 MVU、结算
  -> 创建真实 assistant 楼层并写入 data
  -> 刷新受影响楼层、清理忙碌状态
```

`generate()` 的 Promise 与生成结束事件必须由同一个 transaction 的幂等锁保护，确保只提交一次 assistant 楼层。失败、取消或质量检查拒绝时，只能删除当前事务创建且尚未配对的 user 楼层。

## 6. 无记忆提示词策略

`prompts/native-history-policy.js` 将生成一个固定 system 注入，不执行检索、不写存储、不携带旧快照：

```text
本轮不提供自定义长期记忆、核心记忆或 RAG 召回。请仅依据系统规则、世界书、当前原生聊天记录与本次用户输入推进剧情。未出现在当前上下文中的既往信息不得擅自补造；信息不足时应以当前可见事实为准。
```

该提示用于明确移除记忆功能后的模型行为边界，不能成为变相的记忆载体。

## 7. 工作项与状态

| ID | 工作项 | 状态 | 完成记录 |
| --- | --- | --- | --- |
| P0 | 建立迁移项目文档、冻结架构边界 | 完成 | 2026-09-17：创建本文档；确认现有半成品缺失 `cryptLord.nativeFloorBridge` 与原生输入 DOM 绑定。 |
| P1 | 建立共享宿主 API、生命周期和模块契约层 | 完成 | 2026-09-17：新增 `shared/host-api.js` 与 `shared/lifecycle.js`，并在 loader 中于业务模块前加载。 |
| P2 | 完成真实楼层编排器与生成桥接 | 进行中 | 2026-09-17：加入最小原生生成桥接、无自定义记忆提示和 Promise 提交回退；游戏特有上下文与结算仍待迁入。 |
| P3 | 接入前端输入、行动选项与酒馆原生输入 | 完成 | 2026-09-17：P3-A 统一原生与前端文本输入；P3-C 将模型行动选项附着至真实 assistant 楼层，点击后只填入原生输入框、不自动发送。 |
| P4 | 将状态/MVU/编辑迁移至真实 assistant 楼层 | 完成 | 2026-09-17：状态、状态卡、正文编辑与本楼层 `data` JSON 编辑均已落到真实 assistant 楼层。 |
| P5 | 迁移并模块化保留的游戏前端功能 | 完成 | 2026-09-17：清理全部无业务占位入口；保留模块均有真实楼层职责，其他功能等待独立审计后再重建。 |
| P6 | 删除私有历史、存档、时间线与相关 UI | 完成 | 2026-09-17：生产脚本审计确认只使用真实酒馆楼层，不含私有历史、快照或时间线。 |
| P7 | 删除两套记忆及其提示、设置、调用链 | 完成 | 2026-09-17：生产脚本不含短期/核心/RAG/向量记忆链，并固定注入无自定义记忆提示。 |
| P8 | 静态审计、运行验证与迁移说明收尾 | 进行中 | 2026-09-17：静态审计完成；等待真实 SillyTavern 会话执行端到端验收。 |

## 8. 已知现状

- `modules/native-floor/index.js` 已实现 transaction、user/assistant 楼层创建、幂等提交、重试与孤立 user 楼层清理，但会等待尚不存在的 `cryptLord.nativeFloorBridge`。
- `modules/input-adapter/index.js` 当前只会把调用转交给 `native-floor`，没有监听或拦截 `#send_textarea` 等酒馆原生输入控件。
- `modules/legacy-tools-hub/`、`legacy-battle-launcher/` 等是可见性或壳层迁移，不代表原业务逻辑已迁入。
- 原正则的核心 `handleAction`、上下文、生成结束、结算与编辑逻辑主要位于分块 002；迁移时须以职责拆分，不将原单体对象整体复制到新项目。

## 9. 完成同步规则

每完成一个工作项，必须在本节追加：

- 完成日期；
- 修改文件；
- 保留/删除的行为；
- 静态检查与运行验证；
- 遗留风险和下一工作项。

未更新本文档的实现不得视为该工作项完成。

### P1 完成同步（2026-09-17）

- 修改文件：`shared/host-api.js`、`shared/lifecycle.js`、`loader.js`、本文档。
- `host-api` 只封装 `TavernHelper` 的聊天楼层、生成和 MVU 等公开能力；它没有调用任何写操作，业务模块仍须显式决定何时写入。
- `lifecycle` 为 DOM 监听、酒馆事件和定时器提供归属 scope 与反向清理顺序，供输入适配和业务 UI 安全卸载。
- loader 已验证两个共享模块的 API 形状后才会继续加载业务模块。
- 未做运行期酒馆验证：本工作区不含可连接的 SillyTavern 页面；已按本仓库 `@types` 声明进行静态核对。
- 下一项：P2，将原正则中保留的发送前同步、上下文、生成完成校验和状态结算拆入 `generation-bridge`，并接通现有 `native-floor` 事务模块。

### P2 进行中记录（2026-09-17）

- 新增 `prompts/native-history-policy.js`：提供无自定义长期记忆、核心记忆和 RAG 召回的系统提示；该模块不读取或保存任何记忆数据。
- 新增 `core/generation-bridge.js`：以真实聊天历史作为生成历史，构建最小 `nativeFloorBridge`，并将 MVU 解析后的数据写入本次 assistant 楼层。
- `native-floor` 以 `generate()` Promise 返回作为唯一提交路径；生成结束事件若能提供匹配 generation ID，仅缓存最终文本供 Promise 返回值缺失时回退。因此不会因缺少事件绑定而遗失 assistant 楼层，也不会因事件早到而提前关闭事务。
- 当前桥接尚未迁入原正则的地图、角色档案、系统指令、变量 AI、格式人工修复、奖励结算和界面刷新逻辑，不能视为可替代完整游戏脚本的版本。

### 基线提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 与本文档。
- 提交说明：原生楼层迁移基础设施、无自定义记忆提示与最小生成桥接。
- 推送状态：仅本地提交，不推送远端。

### P3-A 完成同步（2026-09-17）

- 修改文件：`modules/input-adapter/index.js`、本文档。
- 接入范围：酒馆原生 `#send_textarea` / `#send_but` / `#send_form`，以及前端预留的 `#quick-send-input` / `#btn-quick-send`。
- 发送方式：在宿主页面捕获阶段阻止默认发送，并将文本统一交给 `nativeFloor.submitNativeTurn`；成功完成后才清空原输入框并发送 `input` 事件。
- 兼容性：宿主页面以同源 `window` / `parent` / `top` 评分选择；事件目标不使用跨 iframe 不可靠的 `instanceof Element` 判断。
- 失败策略：找不到输入控件或原生楼层编排器不可用时显示明确错误，不回退到旧正则生成路径。
- 静态验证：`node --check`、关键选择器搜索、`git diff --check` 通过。
- 待运行验证：需在真实 SillyTavern 页面确认 Enter 发送偏好、发送按钮、表单提交和流式生成下均只创建一对真实楼层。
- 下一项：P3-B，在行动选项与其他叙事功能模块迁入后，将它们逐项分类为 `narrative-turn` 或 `in-place` 并接入对应执行器。

### P3-A 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P3-A 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P4-A 完成同步（2026-09-17）

- 修改文件：`core/state-store.js`、`core/generation-bridge.js`、`loader.js`、本文档。
- 状态来源：只从当前 user 楼层之前的最近真实 assistant 楼层读取 `data`，不读取 `chatHistoryCache` 或 0 号消息。
- 状态落点：MVU 解析完成后的数据只写入本次真实 assistant 楼层，默认刷新范围为 `affected`。
- 静态验证：`node --check`、loader 顺序检查、`git diff --check` 通过。
- 待运行验证：在真实酒馆页面验证新 assistant 楼层能继承上一 assistant 楼层状态，并且编辑、删除、重生成后读取对象正确变化。
- 下一项：P4-B，将游戏内编辑器与浮动变量编辑器改为使用 `state-store` 的真实 assistant message ID。

### P4-A 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P4-A 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P4-B 完成同步（2026-09-17）

- 修改文件：`core/state-store.js`、`modules/floating-variable-editor/index.js`、`loader.js`、本文档。
- `state-store` 新增按真实 `message_id` 读取楼层 `data` 的接口。
- 浮动状态卡优先读取对应真实 assistant 楼层的 `data.stat_data`；原有 MVU 读取仅作为旧会话兼容回退。
- 状态卡渲染不再因 MVU 未就绪而直接跳过，新的原生楼层可在未注册旧 MVU 全局契约时显示状态。
- 静态验证：`node --check`、旧私有历史字段搜索、`git diff --check` 通过。
- 下一项：P4-C，迁移游戏内编辑器与变量写回，使其通过真实 assistant `message_id` 修改 `message` / `data`。

### P4-B 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P4-B 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P4-C1 完成同步（2026-09-17）

- 修改文件：`core/state-store.js`、`core/native-editor.js`、`loader.js`、本文档。
- 新增原生编辑服务：按真实 assistant `message_id` 打开目标楼层，保存时只更新该楼层的 `message` 与 `data`，刷新范围为 `affected`。
- 默认编辑目标为最近一条真实 assistant 楼层；user 楼层和不存在的楼层会被拒绝。
- 静态验证：`node --check` 与 `git diff --check` 通过。
- P4-C2 待办：为该服务提供宿主页面编辑界面，并迁入原编辑器中可保留的标签编辑体验；不得恢复私有快照或时间线导航。

### P4-C1 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P4-C1 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P4-C2 完成同步（2026-09-17）

- 修改文件：`modules/native-floor-editor/index.js`、`modules/native-floor-editor/style.css`、`loader.js`、本文档。
- 新增宿主页面编辑界面：悬浮按钮打开最近真实 assistant 楼层；保存经 `nativeEditor.save` 更新正文与原 `data`，不创建新楼层。
- 编辑界面不读取私有快照、历史导航或 0 号消息；找不到 assistant 楼层时显示错误并不写入。
- 静态验证：`node --check` 与 `git diff --check` 通过。
- 待运行验证：真实 SillyTavern 页面中确认悬浮按钮位置、编辑保存、取消关闭，以及 assistant 楼层局部刷新。

### P4-C2 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P4-C2 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-A 完成同步（2026-09-17）

- 修改文件：`prompts/context-builder.js`、`core/generation-bridge.js`、`loader.js`、本文档。
- 新增上下文构建器：地图、角色档案、系统规则等保留功能须以显式 provider 注册；生成桥接不再自行拼接上下文。
- 无记忆保证：构建器固定注入 `native-history-policy`，且没有短期经历、核心记忆、RAG、私有快照或向量检索入口。
- 静态验证：`node --check` 与 `git diff --check` 通过。
- 下一项：P2-B，从原正则迁入经审计的地图、角色档案和系统指令上下文提供者；每个提供者必须证明不读取已删除的记忆链路。

### P2-A 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-A 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-B 完成同步（2026-09-17）

- 修改文件：`prompts/game-context-providers.js`、`core/generation-bridge.js`、`loader.js`、本文档。
- 新增地图上下文提供者：仅从 `GameDBManager.DB.mapData` 和上一条真实 assistant 楼层的 `data.stat_data` 读取区域、地标、坐标与移动约束；不读取私有聊天缓存、快照或任何记忆库。
- 新增系统指令提供者：仅依照本轮真实楼层状态中已启用的状态标签，通过 `GameDBManager.getDirectiveByNameAndScope` 读取对应规则，并作为生成注入提供。
- 原正则中的角色档案上下文已审计；该实现本身被禁用，故本阶段明确不迁移，也不以其他数据源替代，避免意外恢复隐性人物记忆。
- 无记忆保证：提供者与生成桥接未引用 `chatHistoryCache`、RAG、`ragAi`、核心记忆、短期经历、检索或向量引擎；上下文构建仍固定先注入无自定义记忆策略。
- 静态验证：`node --check public/guimizhizhu_ui/prompts/game-context-providers.js`、`node --check public/guimizhizhu_ui/core/generation-bridge.js`、`node --check public/guimizhizhu_ui/loader.js`、旧记忆标识搜索及 `git diff --check` 通过。
- 待运行验证：需在真实 SillyTavern 会话中确认地图与状态指令会随当前真实 assistant 楼层 `data` 更新，并确认缺少数据库时安全跳过注入。
- 下一项：继续审计原正则中与游戏规则、结算有关的功能；只迁入不依赖私有历史或记忆链的部分。

### P2-B 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-B 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-C 完成同步（2026-09-17）

- 修改文件：`core/response-normalizer.js`、`core/generation-bridge.js`、`modules/native-floor/index.js`、`loader.js`、本文档。
- 新增脚本化回复规范器：优先从 `<gametxt>` 提取可读剧情，并移除思维链、变量补丁、世界运行日志与仅供旧 UI 使用的协议区块；真实 assistant 楼层因此只保存玩家和模型都可阅读的正文。
- 状态解析仍使用原始模型回复：`native-floor` 先创建可读 assistant 楼层，再将原始协议文本交给 MVU 解析并写入同一楼层 `data`。这避免隐藏协议重新进入原生聊天历史，同时不丢失当前回合的变量更新。
- 边界：本阶段不迁移 `wlog` 跨回合继承，也不保留 `<thinking>`、`<UpdateVariable>` 或 `<JSONPatch>` 作为历史正文；它们不是原生聊天叙事的一部分，也不能替代被移除的记忆系统。
- 静态验证：`node --check` 覆盖新模块、生成桥接、原生楼层和 loader；以含 `thinking`、`gametxt`、`wlog`、`UpdateVariable` 的样例验证规范化结果为纯正文，且原始协议文本仍供解析链使用；`git diff --check` 通过。
- 待运行验证：需在真实 SillyTavern 会话确认带 MVU 协议的回复会显示为可读剧情，且同一 assistant 楼层仍获得解析后的 `data`。
- 下一项：继续审计变量结算；仅迁入能以当前 assistant 楼层 `data` 为唯一状态载体的结算能力，不迁入后台变量 AI、私有快照、世界线回退和记忆摘要调用链。

### P2-C 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-C 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P3-B 完成同步（2026-09-17）

- 修改文件：`core/response-normalizer.js`、`core/generation-bridge.js`、`modules/action-options/index.js`、`modules/action-options/style.css`、`loader.js`、本文档。
- 行动选项来源：从本次模型原始回复的 `<action>` 区块解析、去序号和去重后，只以轻量 `cryptLord.actions` 元数据写入对应真实 assistant 楼层的 `data`；不保存私有快照、时间线或完整协议文本。
- 交互方式：选项显示在其所属真实 assistant 楼层下方；点击后调用 `nativeFloor.submitNativeTurn(action, { source: 'action-option' })`，因此同样只创建一条真实 user 楼层和一条真实 assistant 楼层。
- 删除行为：旧私有行动面板的“临时编辑、删除、填入旧输入框”没有迁入。需要修改行动时，玩家可直接在酒馆原生输入框输入，再经统一输入适配器发送。
- 静态验证：`node --check` 覆盖正文规范器、生成桥接、原生楼层、行动选项模块与 loader；动作样例验证正文与选项正确分离、重复选项会去重；`git diff --check` 通过。
- 待运行验证：需在真实 SillyTavern 页面确认 assistant 楼层写入 `data` 后触发选项渲染，点击选项仅创建一对原生楼层，切换聊天和删除消息会移除关联按钮。
- 下一项：回到 P2 的变量结算审计，评估哪些规则可完全依托本条 assistant 楼层的 `data` 运行；所有私有快照、回退、记忆摘要和后台检索调用维持删除状态。

### P3-B 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P3-B 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P4-C3 完成同步（2026-09-17）

- 修改文件：`core/native-editor.js`、`modules/native-floor-editor/index.js`、`modules/native-floor-editor/style.css`、本文档。
- 原生编辑器现同时展示目标 assistant 楼层的可读正文与 `data` JSON。保存时先要求 JSON 顶层为对象，再通过 `nativeEditor.save` 写回同一条真实 assistant 楼层，刷新范围仍是 `affected`。
- 作用域：编辑器不会读取或写入 0 号消息、`chatHistoryCache`、私有时间线、世界书总结或任何记忆/RAG 数据；状态修改不影响其他楼层。
- 删除行为：原正则的 JSON Patch 纠错面板、AI 辅助修复、后台变量 AI 重算、快照回滚与“写入本周目经历”没有迁入。当前提供的是可审计的直接 JSON 编辑，避免重新建立第二条状态链。
- 静态验证：`node --check public/guimizhizhu_ui/core/native-editor.js`、`node --check public/guimizhizhu_ui/modules/native-floor-editor/index.js` 与 `git diff --check` 通过。
- 待运行验证：需在真实 SillyTavern 页面确认编辑器能显示目标楼层数据、拒绝数组/无效 JSON、保存后只刷新该 assistant 楼层及其状态卡。
- 下一项：继续 P2 结算功能审计，优先考虑可由当前楼层 `data` 与酒馆公开 API 安全完成的规则；不恢复后台变量 AI 或私有存档链。

### P4-C3 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P4-C3 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-D 完成同步（2026-09-17）

- 修改文件：`core/native-settlement.js`、`core/generation-bridge.js`、`loader.js`、本文档。
- 新增原生结算模块：MVU 使用原始回复解析出本回合状态后，在写入真实 assistant 楼层前比较上一条 assistant 楼层 `data` 与新状态；仅有实际状态变动时，才调用现有 `TimePassageEngine.applyTimePassage(data)` 与 `checkCollectionGrowth(data)`。
- 状态边界：结算器直接处理本条 assistant 楼层将写入的 `data` 对象，不读 0 号消息、私有快照、聊天缓存、世界书摘要或记忆检索结果。`cryptLord` 楼层元数据不参与变更比较，因此行动选项本身不会触发时间结算。
- 容错：外部时间引擎或提醒函数缺失、抛错时只记录警告并保留当前状态写入，不能导致原生楼层事务失败。
- 尚未迁入：`updateAcquiredAbilities`、回合资源奖励和依赖旧 UI 的提示逻辑仍待逐项确认公开依赖与状态归属；后台变量 AI、私有回退、快照固化和所有记忆调用链继续排除。
- 静态验证：`node --check` 覆盖结算模块、生成桥接与 loader；行为样例验证相同状态零调用、变更状态各调用一次时间流逝与增殖提醒；`git diff --check` 通过。
- 待运行验证：需在真实会话验证 MVU 状态变化会更新本 assistant 楼层并触发已加载的规则引擎，且纯正文回合不会产生结算副作用。
- 下一项：审计能力补全与奖励计算使用的公开接口，能脱离旧 UI、私有历史和记忆系统的部分才可迁入 P2-E。

### P2-D 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-D 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P3-C 完成同步（2026-09-17）

- 修改文件：`modules/input-adapter/index.js`、`modules/action-options/index.js`、`loader.js`、本文档。
- 行动选项交互已按要求调整：点击选项只通过 `inputAdapter.setInputText(action, 'sillytavern-native')` 写入并聚焦酒馆原生输入框，触发输入事件以同步宿主状态；不会调用 `nativeFloor.submitNativeTurn`，也不会立即创建 user 或 assistant 楼层。
- 统一发送边界保持不变：玩家确认并点击酒馆原生发送按钮、按 Enter，或使用前端发送控件后，才会由输入适配器进入原生楼层事务并创建一对真实楼层。
- 架构调整：`setInputText` 成为输入适配器公开能力，行动选项不再自行定位或操作宿主输入控件，避免跨 iframe 重复实现。
- 静态验证：`node --check` 覆盖输入适配器、行动选项模块与 loader；关键路径搜索确认行动选项模块未引用 `submitNativeTurn`，仅引用 `setInputText`；`git diff --check` 通过。
- 待运行验证：在真实 SillyTavern 页面点击选项后，确认文本进入 `#send_textarea` 且不新增楼层；随后手动发送，确认只新增一条真实 user 与一条真实 assistant 楼层。
- 下一项：继续 P2-E，审计能力补全与回合奖励的公开依赖；不恢复记忆、检索、私有快照或后台变量 AI。

### P3-C 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P3-C 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-E 完成同步（2026-09-17）

- 修改文件：`core/native-settlement.js`、本文档。
- 扩展原生结算：在状态发生真实变化后，结算器仅基于当前 assistant 楼层 `data.stat_data` 的“当前序列”和“序列能力列表”，通过公开 `fetchAvailableAbilities` 补齐缺失的能力；同名能力不会重复添加。
- 兼容现有规则：若存在 `PlayerProjectionSync.syncAll` 或 `AudienceImaginationCore.syncPlayerProjections`，会在能力同步后对同一状态对象执行投影同步；序列为空或“普通人”时只尝试投影同步。
- 容错与边界：能力查询服务或投影服务缺失、失败时安全跳过；不读取私有聊天缓存、时间线、世界书记忆摘要或 RAG。旧 UI 专用的能力弹窗、强制刷新按钮和锁定面板未迁入。
- 尚未迁入：星界之尘与剧场点数奖励依赖旧抽卡/剧场状态管理，尚无原生楼层归属，继续不迁入；后台变量 AI、私有回退和所有记忆调用链仍保持删除。
- 静态验证：`node --check public/guimizhizhu_ui/core/native-settlement.js` 通过；模拟 `fetchAvailableAbilities` 验证缺失能力写入当前状态且返回 `sequence-abilities` 结算标记；`git diff --check` 通过。
- 待运行验证：需在真实会话确认模型更新序列后，最新 assistant 楼层的 `data.stat_data.序列能力列表` 会获得缺失能力且不会重复写入。
- 下一项：继续审计奖励与独立前端功能的状态归属；只有能放入真实楼层数据或明确脚本变量的数据才可迁移。

### P2-E 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-E 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P2-F 完成同步（2026-09-17）

- 修改文件：`core/native-settlement.js`、`core/generation-bridge.js`、`modules/native-floor/index.js`、本文档。
- 回合奖励已迁入原生楼层：本次 user 文本与最终保存的可读 assistant 正文合计每 250 字奖励 1 星界之尘、每 2000 字奖励 1 剧场点数。隐藏协议、思维链和变量补丁不参与字数统计。
- 状态归属：星界之尘累计在当前 assistant 楼层的 `data.cryptLord.rewards.astralDust`，并随下一条 assistant 楼层状态继承；剧场点数累计在同一楼层的 `data.stat_data.剧场点数`。两者不再写入旧 `AppStorage`、0 号消息或私有快照。
- 执行时机：奖励只在原生叙事事务成功提交时结算一次；重新打开编辑器、渲染状态卡或点击行动选项不会重复结算。
- 删除/保留边界：旧抽卡 UI、抽卡记录、伙伴收藏、世界线回退和私有存档不随本阶段迁入。该阶段只保留与真实回合关联的数值奖励，后续抽卡前端若迁移，必须读取楼层数据而非创建平行存储。
- 静态验证：`node --check` 覆盖结算模块、生成桥接和原生楼层；奖励样例验证 2000 个可读字符获得 8 星界之尘、1 剧场点数，并正确写入当前楼层数据；`git diff --check` 通过。
- 待运行验证：需在真实会话验证一次完成回合只结算一次，且下一回合能够继承星界之尘和剧场点数。
- 下一项：审计现有 `legacy-*` 入口与抽卡/剧场等前端模块，逐项决定迁移、重建或明确删除，保持项目树和状态边界整洁。

### P2-F 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P2-F 改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P5-A 完成同步（2026-09-17）

- 修改文件：`loader.js`、`shared/debug.js`、删除 `modules/legacy-battle-launcher/`、删除 `modules/legacy-tools-hub/`、本文档。
- 删除原因：两个 legacy 模块均只是旧 UI 的可见入口壳，点击后不执行可用业务；继续加载会向玩家展示无法完成的功能，也违背项目树干净、模块职责明确的要求。
- 删除范围：移除战斗悬浮入口、工具与衍生玩法面板、其 CSS、loader 注册与调试模块观察项。Git 历史保留原壳层实现，未来若完成真实业务审计可重新以独立模块建立。
- 保留边界：没有删除原生楼层、输入、行动选项、状态卡、编辑器、判定美化或结算模块。调试面板现观察 `actionOptions`、`nativeEditor`、`nativeFloorEditorUi` 与 `nativeSettlement` 等实际模块。
- 静态验证：`node --check public/guimizhizhu_ui/loader.js`、`node --check public/guimizhizhu_ui/shared/debug.js` 通过；legacy 模块键/路径全文搜索无匹配；`git diff --check` 通过。
- 待运行验证：需在真实页面确认加载器不再请求已删除资源，且不再出现无功能的战斗/工具浮动入口。
- 下一项：P5-B，逐项审计尚存 `judgment-beautify` 等模块的真实数据依赖与楼层归属；不可运行的壳层不再保留。

### P5-A 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P5-A 清理改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P5-B 完成同步（2026-09-17）

- 修改文件：`loader.js`、`shared/debug.js`、删除 `modules/judgment-beautify/`、本文档。
- 删除原因：`judgment-beautify` 仅返回“尚未迁移”的拒绝结果，没有任何真实 assistant 楼层的读取、监听或渲染实现；它属于无功能占位，不应作为可加载模块保留。
- 删除范围：移除判定美化 CSS、loader 注册、调试模块观察项和空模块目录。未来只有在判定协议、数据来源、显示位置与交互边界均审计完成后，才建立新的功能模块。
- 静态验证：`node --check public/guimizhizhu_ui/loader.js`、`node --check public/guimizhizhu_ui/shared/debug.js` 通过；判定模块键/路径全文搜索无匹配；`git diff --check` 通过。
- 待运行验证：需在真实页面确认加载器不再请求判定美化资源，且调试面板不显示未迁移模块。
- 下一项：P5-C，审计状态卡、行动选项和编辑器以外的剩余可见模块；继续删除无业务壳，或只迁入真实楼层可验证的功能。

### P5-B 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P5-B 清理改动与本文档。
- 推送状态：仅本地提交，不推送远端。

### P5-C 完成同步（2026-09-17）

- 修改文件：`shared/debug.js`、本文档。
- 调试模块清单加入 `nativeFloorBridge`，限制信息改为当前真实架构：原生楼层、输入适配、行动选项、状态卡、编辑器和结算已注册；运行期风险仅保留为真实 SillyTavern 会话中的端到端验证。
- 删除过期诊断：不再声称原生生成桥接、变量写入或战斗/工具占位入口仍已加载。战斗、判定、工具中心、抽卡与剧场仍未迁入时，会明确显示为“已删除旧占位入口”，而不是伪造功能可用。
- 模块审计：保留的可见模块都具有真实楼层读写或显示职责；`legacy-*`、`judgment-beautify`、`stage1-skeleton` 等占位标识均已不在生产目录中。
- 静态验证：`node --check public/guimizhizhu_ui/shared/debug.js` 通过；旧占位、私有历史与记忆检索标识全文搜索无匹配；`git diff --check` 通过。
- 下一项：P6/P7 进行删除验收，确认生产目录不再包含私有历史、时间线、短期/核心记忆、RAG 或向量检索调用；随后进入 P8 的运行期验收。

### P5-C 提交记录（2026-09-17）

- 提交范围：`public/guimizhizhu_ui/` 的 P5-C 诊断校正与本文档。
- 推送状态：仅本地提交，不推送远端。

### P6 完成同步（2026-09-17）

- 验收范围：`public/guimizhizhu_ui/` 的生产脚本，不包含作为迁移依据保留在 `数据库前端/` 的原正则分块资料。
- 真实历史来源：`native-floor` 与 `state-store` 只调用酒馆公开聊天楼层 API；user 和 assistant 每回合均为真实楼层，状态从最近真实 assistant 楼层 `data` 继承。
- 删除确认：未发现 `chatHistoryCache`、`historyViewIndex`、`isHistoryViewMode`、`renderHistoryControls`、私有快照、自动存档、`AppStorage` 或 `timeline_history` 调用。
- UI 清理：无功能的战斗、工具中心、判定占位入口已在 P5 删除，因此不会误导玩家操作不存在的私有时间线或旧存档功能。
- 静态验证：私有历史/时间线/存档标识全文搜索无匹配；当前模块树仅保留原生楼层、输入、编辑、状态与实际调试模块。

### P6 提交记录（2026-09-17）

- 提交范围：P6 删除验收记录与本文档。
- 推送状态：仅本地提交，不推送远端。

### P7 完成同步（2026-09-17）

- 删除确认：生产脚本中未发现 `_retrieveRelevantMemories`、`_assembleShortTermMemory`、`_assembleCoreMemory`、`RAG_AI_KEYS`、`vectorEngine`、`ragAi`、核心记忆写入或经历摘要写入调用。
- 提示词边界：`native-history-policy.js` 固定提示模型“不提供自定义长期记忆、核心记忆或 RAG 召回”，并要求只依据系统规则、世界书、当前原生聊天记录和本次输入。
- 上下文边界：`context-builder` 始终先注入该固定提示；现有提供者仅提供地图与系统指令，不读取记忆库、私有聊天缓存或向量索引。
- 兼容说明：生产代码中出现的“私有历史/记忆/RAG”仅用于调试面板说明该能力已删除，不代表存在数据读取或调用链。
- 静态验证：记忆/检索/向量相关标识搜索仅命中固定删除提示与诊断文案；无可执行调用。

### P7 提交记录（2026-09-17）

- 提交范围：P7 删除验收记录与本文档。
- 推送状态：仅本地提交，不推送远端。

## 10. 验收清单

- 前端输入发送一次：恰好新增一条真实 user 与一条真实 assistant 楼层。
- 酒馆原生输入发送一次：同样恰好新增一对楼层，无默认发送导致的重复 user 楼层。
- 酒馆原生编辑、删除、重生成与滚动能直接作用于真实楼层。
- 编辑器只更新目标 assistant 楼层的 `message` 和 `data`。
- 生产代码不再引用 `chatHistoryCache`、`historyViewIndex`、`isHistoryViewMode`、`renderHistoryControls`、`_retrieveRelevantMemories`、`_assembleCoreMemory`、`RAG_AI_KEYS`、`writeSummaryToLorebook`。
- 正文生成请求不包含近期经历切片、核心记忆或 RAG 召回；RAG API 不会被调用。
