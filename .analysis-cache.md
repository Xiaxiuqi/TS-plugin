<analysis>
  <context>目标项目边界为 public/story_ui_lite_test/**；地图 AI 配置与日志链路集中在 public/story_ui_lite_test/index.js 与 public/story_ui_lite_test/modules/db-status-bar/index.js；当前 URL 摘要会去掉 query/hash，不满足测试版完整 URL 诊断；当前 API key 只输出 present/tail，不能改成完整输出。</context>
  <needs>测试版日志输出完整 URL 与当前模型；不输出完整 API key；为后续脱敏日志提供只显示当前模型的结构；同步项目文档；完成语法检查、敏感日志审计、高性能模型审计和 commit。</needs>
  <approach>在两个 JS 文件内新增 URL 内密钥参数脱敏 helper，把 summarizeMapUrl 改为输出完整脱敏 URL；新增模型-only summary；custom_api 与主 API 调用日志补 currentModel/modelSource；不修改 prod，不引入配置系统。</approach>
  <edge_cases>空 URL、合法 URL 带 query/hash、URL query 含 key/token/secret/password、非法 URL 字符串、custom_api 只有 model、主 API 模型不可见、AI 返回空/非字符串、模型拉取失败。</edge_cases>
  <affected_scope>public/story_ui_lite_test/index.js; public/story_ui_lite_test/modules/db-status-bar/index.js; public/story_ui_lite_test/PROJECT.md; public/story_ui_lite_test/STATUS_BAR_PLAN.md; .analysis-cache.md; .analysis-archive/*</affected_scope>
  <execution_plan>补读模型拉取段；修改 index.js 管理日志摘要；修改 db-status-bar/index.js 运行时日志摘要与调用日志；同步文档；执行 node --check、敏感日志搜索、git diff；高性能模型审计闭环；commit。</execution_plan>
  <degradation_check>只改测试版路径；不输出完整 API key；不伪造主 API 模型；不用 shell 修改源码；不新增不必要抽象；执行验证与审计。</degradation_check>
</analysis>
---
<decision_point>
  <issue>.analysis-cache.md 首次写入不完整。</issue>
  <impact>验收依据缺失，必须先修复缓存再继续业务修改。</impact>
  <recommendation>采用结构完整的压缩版 analysis cache，并记录本决策；不改变业务目标。</recommendation>
  <execution_plan_update>继续修改测试版管理与运行时地图 AI 日志，随后同步文档、验证、审计、commit。</execution_plan_update>
  <degradation_check>只修复流程缓存；不使用 shell 修改源码；不跳过后续验收。</degradation_check>
</decision_point>

---
<decision_point>
  <issue>高性能模型审计阻止 commit：主 API 路径可能伪造数据库当前模型；URL 脱敏漏掉 camelCase 密钥参数。</issue>
  <impact>错误模型会误导排障；accessToken/clientSecret 等参数可能在完整 URL 日志中泄露。</impact>
  <recommendation>选择修正模型摘要语义和敏感 query key 判断函数：主 API 不可见则 unavailable，custom_api 才输出实际传入模型；URL 参数覆盖 snake/kebab/plain/camelCase/PascalCase。</recommendation>
  <execution_plan_update>修改两个 JS 文件与文档，重新验证并再次高性能审计，通过后再 commit。</execution_plan_update>
  <degradation_check>不使用 shell 修改源码；不提交未通过审计的代码；不把历史配置模型伪装成主 API 当前模型。</degradation_check>
</decision_point>

---
<decision_point>
  <issue>二次高性能审计阻止 commit：异常路径直接输出原始 error 或未脱敏 message，可能泄露完整 API Key、URL、custom_api 或 headers。</issue>
  <impact>结构化日志脱敏会被 catch/fail 裸输出绕过，安全验收不能通过。</impact>
  <recommendation>新增文本级敏感信息脱敏与 error 摘要；所有异常日志不再输出原始 error 对象，message/String(error) 必须先脱敏。</recommendation>
  <execution_plan_update>修改两个 JS 文件异常日志路径，同步文档，重跑验证和高性能审计，通过后再 commit。</execution_plan_update>
  <degradation_check>保留测试版诊断价值；不使用 shell 修改源码；不提交未通过审计的代码。</degradation_check>
</decision_point>
